# Configuration

## Purpose
Environment configuration, options and external secrets.

## Mandatory Rules
Do not commit secrets; respect configured security, logging and provider settings rather than building runtime values from request host data.

## Load Next
[Security](Security.md), [OAuth](OAuth.md), [Operations](Operations.md).
