# Events

## Purpose
Event contracts, dispatch and handler boundaries.

## Mandatory Rules
Use `EventBase` and DI dispatch; preserve correlation IDs and avoid static email-coupled publishers.

## Load Next
[Logging](Logging.md), [Outbox](Outbox.md), [Framework events](Framework/CBPEvents.md).
