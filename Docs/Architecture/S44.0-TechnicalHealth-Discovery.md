# S44.0 — Technical-Health Discovery (PassPlat + CBP + InventaNet)

**Date:** 2026-08-21
**Mode:** READ-ONLY discovery. No `.cs/.sql/.csproj/.slnx/.json/test/DB` was modified.
**Deliverable:** This discovery document only (spec §45 format). S44.0 = CLOSED/DISCOVERY. S44.1/2/3 NOT authorized.
**MCP workflow:** `sequential-thinking` (hypotheses) + `supermemory` context (S38.2→S42.3 certifications) already in hand; `sharplens`/`grep` for static evidence.

---

## 1. Scope & Method

Targets:
- `D:\CODIGOS\PassPlat\PassPlat.slnx` (primary)
- `D:\CODIGOS\CBP\CBP.slnx` (framework)
- `D:\CODIGOS\InventaNet\InventaNet.slnx` (external consumer, NO-TOUCH)

Method: baseline build + test (G1–G4, G13) plus targeted static scans (H2,H4,H6,H7) and reference/path verification (H3,H8). SDK locale is Spanish; test summaries parsed from `Correctas/Superado/Total`.

## 2. Evidence (Gates)

| Gate | Command | Result |
|---|---|---|
| G1 PassPlat build | `dotnet build PassPlat.slnx -v q` | 0 Errors, **0 Advertencia(s)**, EXIT=0 |
| G2 PassPlat test | `dotnet test PassPlat.slnx --no-build` | **210/210** (56 arch + 154 app), 0 failed, EXIT=0 |
| G3 CBP build | `dotnet build CBP.slnx -v q` | 0 Errors, EXIT=0 |
| G4 CBP test | `dotnet test CBP.slnx` | CBP.slnx contains **no test projects** (only restore/build ran). The certified 56/56 CBP password baseline is **not reproducible from this checkout** (no `*Test*.csproj` under `D:\CODIGOS\CBP`). |
| G13 InventaNet build | `dotnet build InventaNet.slnx -v q` | 1 Error (MSB3202), EXIT=1 |

**Baseline match:** PassPlat 210/210 == historical baseline (56 architecture + 154 application) → **H1 (regression) = NO**.

## 3. Hypotheses

| H | Area | Scan | Result | Classification |
|---|---|---|---|---|
| H1 | Build/test regression | G1–G4 | PassPlat 210/210 green; CBP builds green | GREEN (no regression) |
| H2 | Multi-tenant context injection | grep `IdTenant` in Aplicacion | `IdTenant` passed as explicit params; **no** `Usuario.IdTenant` used as auth context, **no** `idTenant ?? usuario.IdTenant` anti-pattern | GREEN |
| H3 | CBP broken contracts | PassPlat compiles against CBP packages | Compile-time contract intact (PassPlat builds 0 err) | GREEN |
| H4 | Dependency skew / NU1903 | grep `Directory.Packages.props` + 0 build warnings | EF Core **10.0.11**, SqlClient **7.0.2**, IdentityModel.Tokens **8.22.0** identical in PassPlat & CBP; 0 NU1903 warnings | GREEN (aligned) |
| H5 | Email / correlation | scans + S41 cert | `EmailQueue.EnqueueAsync` centralizes CorrelationId; S41 certified 44/45 W3C IDs | GREEN (resolved) |
| H6 | Logging EventName guard | grep `EventName` | All `EventName = LoggingEvents.X` (constants) or variables; `RoslynEventNameCatalogGuard` + `EventNameCatalogGuardTests` present | GREEN (guard active) |
| H7 | OAuth violations | grep `RedirectUri`/`new HttpClient`/`ConcurrentDictionary`/`.Wait()`/empty `catch` | `RedirectUri = conf.Callback` (persisted, compliant); DI `AddHttpClient()` only; **no** `ConcurrentDictionary` OAuth state; **no** sync-over-async; **no** empty `catch` | GREEN |
| H8 | InventaNet consumer impact | G13 + path checks | InventaNet references `../CBPNet/CBP.Excel/CBP.Excel.csproj`; `CBPNet` dir **does not exist** (nor `CBP\CBP.Excel`) | **NO-TOUCH / environmental** |

## 4. STOP-Condition Adherence

- External consumer boundary hit at G13 → **documented, not modified** (InventaNet is NO-TOUCH).
- No circular dependency, breaking change, or data-loss risk detected in PassPlat/CBP code.
- Historical debt explicitly NOT re-flagged: H-2 (MfaService.cs:132), H-3 (S39/S41), BUG-017.1.3 (S42.0/3 no-fix), PBKDF2 100k floor (HashingService.cs Math.Max), S38.2 email templates.

## 5. Findings

1. **G13 — InventaNet build break (reproducible defect).** `InventaNet.slnx` line `<Project Path="../CBPNet/CBP.Excel/CBP.Excel.csproj" />` points to a project/directory that does not exist on this machine (`Test-Path D:\CODIGOS\CBPNet` = False; `Test-Path D:\CODIGOS\CBP\CBP.Excel\CBP.Excel.csproj` = False). Error:
   `C:\Program Files\dotnet\sdk\10.0.203\NuGet.targets(519,5): error MSB3202: No se encuentra el archivo del proyecto "D:\CODIGOS\CBPNet\CBP.Excel\CBP.Excel.csproj".`
   Root cause is a **cross-repo reference/path drift**, not a PassPlat/CBP code defect. Classified **NO-TOUCH / environmental** — out of S44 authorized fix scope; escalate to InventaNet owner.

2. **G4 — CBP test baseline not reproducible (minor DEBT/HARDENING).** The 56/56 CBP `Security.Password.Tests` baseline cited in S42.2/3 is absent from the current `CBP` checkout (`CBP.slnx` has no test projects, no `*Test*.csproj` under `D:\CODIGOS\CBP`). The 56/56 claim cannot be re-verified here.

3. **Carried known issue — BUG-017.1.3 (INTENTIONAL / no-fix).** IdentityModel.Tokens 8.22.0 still in use; the JWT-without-`kid` acceptance (IDX10517) behavior is documented and frozen as no-fix per S42.3. Highest-severity *known* item, but not a new bug.

## 6. Single Most Important Problem & Recommendation

**No acute code defect exists in PassPlat or CBP.** All code-level hypotheses (H1–H7) are GREEN; the 210/210 PassPlat baseline is intact and dependency versions are aligned.

The only **reproducible technical defect** discovered is **G13 (InventaNet MSB3202)** — but it sits on an external-consumer boundary and is therefore NO-TOUCH/escalate, not a PassPlat/CBP sprint item.

**Recommendation:** Close S44.0 with **no new code sprint required** for PassPlat/CBP. Two follow-ups outside the authorized build:
- (a) **Escalate G13** to the InventaNet owner: fix the `CBP.Excel` project reference/path (environmental, NO-TOUCH for PassPlat/CBP).
- (b) **HARDENING spike** (optional, low priority): restore/verify the CBP password-test project into `CBP.slnx`/CI so the 56/56 baseline is reproducible (closes G4 gap).
- (c) **Keep BUG-017.1.3** as documented INTENTIONAL/no-fix; revisit only if a consumer is actually broken by IDX10517.

## 7. Repro Commands

```powershell
cd D:\CODIGOS\PassPlat
dotnet build PassPlat.slnx -v q
dotnet test PassPlat.slnx --no-build
cd D:\CODIGOS\CBP
dotnet build CBP.slnx -v q
dotnet test CBP.slnx
cd D:\CODIGOS\InventaNet
dotnet build InventaNet.slnx -v q
Test-Path 'D:\CODIGOS\CBPNet\CBP.Excel\CBP.Excel.csproj'
```
