# S26.2 — Implementation: F9 Desacoplamiento `PassPlat.Aplicacion → PassPlatDbContext`

| Campo | Valor |
|-------|-------|
| **Tipo** | Reporte de implementación + certificación |
| **Sprint** | S26.2 |
| **Objetivo** | Eliminar `PassPlatDbContext` de `FederacionService` y `OAuthCatalogValidationService` (F9) |
| **Fuente** | `S26.1-Design.md` (DISEÑO APROBADO) |
| **Depende de** | S26.0 Discovery · S26.1 Design |
| **Estado** | ✅ CLOSED / GATE PASS |

---

## 1. Cambios realizados

### 1.1 Read model nuevo (Datos)

**`PassPlat.Datos\Models\DesgloseProveedorReadModel.cs`** (nuevo) — estructura plana de proyección propia de Datos, sin referencias a Aplicación:

```csharp
public class DesgloseProveedorReadModel
{
    public int IdProvIden { get; init; }
    public string Codigo { get; init; } = string.Empty;
    public string Nombre { get; init; } = string.Empty;
    public string? Icono { get; init; }
    public int TotalVinculadas { get; init; }
}
```

### 1.2 3 métodos nuevos en repos existentes

| Método | Repo | Consulta cubierta |
|--------|------|-------------------|
| `ObtenerDesglosePorProveedorAsync(int idTenant, ct)` → `Result<IReadOnlyList<DesgloseProveedorReadModel>>` | `IIdenExtRepository` | F-1 (GroupBy IdenExt + COUNT) |
| `ContarActivosAsync(ct)` → `Result<int>` | `IProvIdenRepository` | F-2 (COUNT ProvIden Activo) |
| `ObtenerTodosAsync(ct)` → `Result<IReadOnlyList<ConfProvIden>>` | `IConfProvIdenRepository` | O-2 (ConfProvIden todos) |

- Todos envuelven la consulta en try-catch con `Result.Failure("DB_ERROR", ex.Message)` (patrón AGENTS.md).
- F-1 preserva `AsNoTracking()`, agrupación por `IdProvIden/Codigo/Nombre/Icono`, y **omite `IgnoreQueryFilters()`** (verificado no-op — no existe `HasQueryFilter`). No materializa entidades `IdenExt` (no expone `AccessToken`/`RefreshToken`/`IdToken` byte[]).
- O-2 preserva `AsNoTracking()`, sin tenant, sin Includes.

### 1.3 Servicios refactorizados (sin `PassPlatDbContext`)

**`FederacionService`** — constructor nuevo:
```csharp
public FederacionService(IIdenExtRepository idenExtRepo, IProvIdenRepository provIdenRepo, IAudIdenExtRepository audIdenExtRepo)
```
- F-1: `ObtenerDesglosePorProveedorAsync` → mapea `DesgloseProveedorReadModel → ProveedorEstadisticasDto`.
- F-2: `ContarActivosAsync`.
- F-3: reutiliza `ObtenerPorTenantAsync(idTenant, limite: 10)` (DEC-2 — Include(ProvIden) preservado) → proyecta a `UltimaActividadFederacionDto`.
- `using Microsoft.EntityFrameworkCore` y `using PassPlat.Datos` eliminados.

**`OAuthCatalogValidationService`** — constructor nuevo:
```csharp
public OAuthCatalogValidationService(IProvIdenRepository provIdenRepo, IConfProvIdenRepository confProvIdenRepo)
```
- O-1: reutiliza `ObtenerTodosOrdenadosAsync` (DEC-4).
- O-2: `ObtenerTodosAsync`.
- 12 reglas de validación **sin cambios de lógica** (solo fuente de datos: repos).
- `using Microsoft.EntityFrameworkCore` y `using PassPlat.Datos` eliminados.

### 1.4 DI

- **Sin cambios estructurales**: `AplicacionDependencyInjection.cs` L141/L148 intactos; los 4 repos ya registrados (`DatosDependencyInjection.cs:77-80`). Solo cambian los constructores de los servicios.

---

## 2. Gate de implementación — verificación física

| # | Criterio | Resultado |
|---|----------|-----------|
| I1 | 0 `PassPlatDbContext` en `FederacionService` | ✅ grep 0 matches |
| I2 | 0 `PassPlatDbContext` en `OAuthCatalogValidationService` | ✅ grep 0 matches |
| I3 | 0 `using Microsoft.EntityFrameworkCore` en los 2 servicios (ni constructores ni usings) | ✅ grep 0 matches en **todo** `PassPlat.Aplicacion` |
| I4 | 5 consultas cubiertas | ✅ matriz §1.2/§1.3 |
| I5 | 0 repositorios nuevos | ✅ 3 métodos añadidos a repos existentes |
| I6 | Datos → Aplicación = 0 | ✅ `DesgloseProveedorReadModel` en `PassPlat.Datos.Models`, sin refs a DTOs App |
| I7 | DTO de Aplicación no cruza hacia Datos | ✅ `ProveedorEstadisticasDto` solo se construye en el servicio |
| I8 | ReadModel pertenece a `PassPlat.Datos` | ✅ `PassPlat.Datos\Models\DesgloseProveedorReadModel.cs` |
| I9 | AsNoTracking preservado | ✅ F-1 y O-2 con `AsNoTracking`; F-2 Count no requiere; F-3 hereda `AsNoTracking` del repo |
| I10 | Include preservado donde DEC-2 lo requiere | ✅ `ObtenerPorTenantAsync` conserva `Include(a => a.ProvIden)` |
| I11 | DI solo cambia constructores | ✅ sin cambios en registros |
| I12 | Comportamiento funcional intacto | ✅ 87/87 xUnit · 56/56 Architecture · build 0 errores |

---

## 3. Evidencia de gates posteriores

| Gate | Resultado |
|------|-----------|
| `dotnet build PassPlat.slnx` | ✅ 0 errores · 23 warnings (pre-existentes: NU1903 + CS8602 `CrearConfProvIdenValidator.cs:51,56` + `ConfProvIdenService.cs:119`) |
| `dotnet test PassPlat.slnx --no-build` — xUnit | ✅ **87/87 PASS** (51s) |
| `dotnet test PassPlat.slnx --no-build` — Architecture | ✅ **56/56 PASS** (47 S24 + 9 S25_2) |
| Regresión funcional | ✅ suites completas sin regresión (cobertura F9 no añade tests nuevos — el contrato lo mantienen repos+servicios) |

---

## 4. Verificación del estado F9

### Antes (S26.0)
```
PassPlat.Aplicacion
    ├── FederacionService ──────────────── PassPlatDbContext (3 consultas EF)
    └── OAuthCatalogValidationService ──── PassPlatDbContext (2 consultas EF)
```

### Después (S26.2)
```
PassPlat.Aplicacion
    ├── FederacionService ──→ IIdenExtRepository + IProvIdenRepository + IAudIdenExtRepository
    └── OAuthCatalogValidationService ──→ IProvIdenRepository + IConfProvIdenRepository
                                        ↓
                                  PassPlat.Datos
                                        ↓
                                    EF Core
```

**Grep exhaustivo post-cambio**:
- `PassPlatDbContext` en `PassPlat.Aplicacion`: **0 matches**
- `Microsoft.EntityFrameworkCore` en `PassPlat.Aplicacion`: **0 matches** (todo el proyecto)
- `DesgloseProveedorReadModel`: solo en `PassPlat.Datos\Models` + `IdenExtRepository` (6 matches, todos en Datos)

---

## 5. Archivos tocados

| Archivo | Cambio |
|---------|--------|
| `PassPlat.Datos\Models\DesgloseProveedorReadModel.cs` | **Nuevo** — read model Datos |
| `PassPlat.Datos\Repositories\IdenExtRepository.cs` | +interfaz +impl `ObtenerDesglosePorProveedorAsync` (F-1) |
| `PassPlat.Datos\Repositories\ProvIdenRepository.cs` | +interfaz +impl `ContarActivosAsync` (F-2) |
| `PassPlat.Datos\Repositories\ConfProvIdenRepository.cs` | +interfaz +impl `ObtenerTodosAsync` (O-2) |
| `PassPlat.Aplicacion\Services\BBDD\FederacionService.cs` | Constructor repos, 0 EF, proyecciones en servicio |
| `PassPlat.Aplicacion\Services\OAuth\OAuthCatalogValidationService.cs` | Constructor repos, 0 EF, lógica intacta |

---

## 6. Veredicto

```
S24   ✅ CLOSED / GATE PASS
S25.2 ✅ CLOSED / GATE PASS
S26.0 ✅ DISCOVERY COMPLETE
S26.1 ✅ DESIGN APPROVED
S26.2 ✅ IMPLEMENTATION AUTHORIZED → CLOSED / GATE PASS (2026-08-14)
S27   ❌ NOT DEFINED / WAIT — F9 demostrado resuelto; S27 sigue separado
```

**F9 CERRADO**: `PassPlat.Aplicacion` queda **100% desacoplada de EF Core**. La frontera `Aplicación → Repositorios/contratos → Datos → EF Core` queda materializada. Regresión OAuth/Federación cubierta por la suite completa (87/87 xUnit + 56/56 Architecture) sin pérdida funcional.