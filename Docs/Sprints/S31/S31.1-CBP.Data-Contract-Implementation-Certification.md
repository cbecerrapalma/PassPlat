# S31.1 — CBP.Data Contract Implementation — Certification

| Campo | Valor |
|-------|-------|
| **Tipo** | Certificación de implementación |
| **Fuente** | `Docs/Sprints/S31/S31.0-CBP.Data-Contract-Discovery.md` (S31.0 veredicto PASS, 2026-08-15) |
| **Depende** | S31.0 DISCOVERY+DESIGN (CLOSED) · S30.0 · S29.2 · S27 · S25.1 |
| **Influye** | S32 (Observability Application — DEUDA-009 + DEUDA-008) |
| **Estado** | ✅ **CLOSED / GATE PASS** (2026-08-15) |
| **Autorizado** | Usuario — 2026-08-15 (alcance mínimo: RETAIN×3 + REMOVE Detach + REMOVE UnitTest1) |

## 1. Objetivo

Ejecutar el alcance mínimo autorizado del contrato CBP.Data:

1. **RETAIN** `GetRepository<T>` / `GetCustomRepository<T>` / `Query()` (Async+Sync) — sin cambio de firma, comportamiento, DI ni fábricas.
2. **REMOVE** `Detach<TEntity>` de `UnitOfWorkSync.cs` (única API eliminada; cierre de asimetría de parity a 0/0).
3. **REMOVE** `PassPlat.Aplicacion.Test/UnitTest1.cs` (test vacío, higiene).

## 2. Alcance exacto de cambios (diff)

| Archivo | Cambio |
|---------|--------|
| `CBP/CBP.Data/CBP.Data.Synchronous/UnitOfWorkSync.cs` | XML docs de `GetRepository`/`GetCustomRepository` actualizados (RETAIN S31, reemplazan "conservado temporalmente") · **eliminado método `Detach<TEntity>` (línea ~241)** |
| `CBP/CBP.Data/CBP.Data.Asynchronous/UnitOfWorkAsync.cs` | XML docs de `GetRepository`/`GetCustomRepository` actualizados (RETAIN S31) |
| `PassPlat/PassPlat.Aplicacion.Test/UnitTest1.cs` | **Eliminado** (test vacío `Test1` sin asserts) |

**NO modificados** (verificado): `CBP.Data.Abstractions` (IUnitOfWork, IRepository, IReadRepositoryAsync/Sync), `UnitOfWorkAsync.cs` (API), repos PassPlat, DI (`AplicacionDependencyInjection.cs`, `DatosDependencyInjection.cs`), InventaNet (NO-TOUCH), CBP.Events, Outbox, chain de emails.

**Backup** (abitación original pre-edición):

```
C:\Users\DEVELO~1\AppData\Local\Temp\opencode\s31.1-backup-20260815-161556\
  UnitOfWorkAsync.cs.bak   11048 B   SHA256 19ED25AB…  (contiene Detach)
  UnitOfWorkSync.cs.bak    10164 B   SHA256 3F592C42…
  UnitTest1.cs.bak           125 B   SHA256 2BFB944C…
```

## 3. Matriz de gates

| Gate | Criterio | Resultado |
|------|----------|-----------|
| G1 | Backup de archivos afectados con hash SHA256 | ✅ Backup íntegro; `.bak` de Sync contiene estado original con `Detach` (295 líneas) |
| G2 | REMOVE `Detach<TEntity>` de `UnitOfWorkSync.cs` | ✅ Eliminado; grep global `public void Detach<TEntity>`/`.Detach<` = 0 refs en CBP |
| G3 | REMOVE `UnitTest1.cs` | ✅ Eliminado; no queda ninguna copia en árbol PassPlat |
| G4 | RETAIN intacto: GetRepository/GetCustomRepository/Query Async+Sync, fábricas, DI, InventaNet sin modificar | ✅ Fábricas `IRepositoryFactoryAsync/Sync` intactas (delegan); `Query()` en `IReadRepository{Async,Sync}.cs:9`; DI sin cambios; InventaNet no tocado |
| G5 | Build CBP + PassPlat 0 errores | ✅ CBP.slnx 0 errores (solo NU1903 CBP.Excel pre-existente) · PassPlat.slnx 0 errores (20 warnings NU1903 pre-existentes) |
| G6 | Tests — baseline menos UnitTest1 | ✅ Architecture **56/56** · Aplicación **96/96** (97 − 1 test vacío eliminado = reducción exacta esperada) · Total **152/152** |
| G7 | Validación InventaNet (NO-TOUCH) | ✅ Único error del slnx: MSB3202 (ruta `CBPNet\CBP.Excel.csproj` inexistente — pre-existente S29.2, propiedad de InventaNet). Con `NoWarn=NU1605` (SqlClient 7.0.1 vs 7.0.2 pre-existente), Inventa.Data compila con **7 errores CS0308 `IUnitOfWorkAsync`** — **idénticos con el UnitOfWorkSync.cs ORIGINAL (swap test)** → 100% pre-existentes, independientes de S31.1. Los 157 usos de `GetCustomRepository` no aparecen como error. |
| G8 | Parity final | ✅ `Detach` = Async ❌ / Sync ❌ (0/0) · `GetRepository` Async ✅ / Sync ✅ · `GetCustomRepository` Async ✅ / Sync ✅ · `Query()` Async ✅ / Sync ✅ |

## 4. Swap test (G7 — evidencia de pre-existencia)

Procedimiento controlado (mismo criterio que S29.2):

```powershell
# 1. Guardar versión editada
Copy-Item UnitOfWorkSync.cs s31.1-swaptest-editado.cs
Copy-Item s31.1-backup\UnitOfWorkSync.cs.bak UnitOfWorkSync.cs   # ORIGINAL pre-S31.1

# 2. Build Inventa.Data con el ORIGINAL (con Detach)
dotnet build Inventa.Data.csproj --no-restore
# → 7 errores CS0308 (IUnitOfWorkAsync no genérico con argumentos de tipo)

# 3. Restaurar versión editada y rebuild
Copy-Item s31.1-swaptest-editado.cs UnitOfWorkSync.cs
dotnet build CBP.Data.Synchronous.csproj   # 0 errores
```

**Conclusión**: los errores de Inventa.Data son pre-existentes (desajuste de su código con la interfaz `IUnitOfWorkAsync` unificada — propiedad del producto, documentado en S29.2). La eliminación de `Detach` no introduce ningún error nuevo.

## 5. Parity final (G8)

| API | Async | Sync | Estado |
|-----|-------|------|--------|
| `GetRepository<T>` | ✅ | ✅ | RETAIN |
| `GetCustomRepository<T>` | ✅ | ✅ | RETAIN |
| `Query()` | ✅ (`RepositoryAsync.cs:21`) | ✅ (`RepositorySync.cs:21`) | RETAIN |
| `Detach<T>` | ❌ (no existió) | ❌ (REMOVED) | 0/0 cierre |

## 6. Decisiones respetadas

- InventaNet NO-TOUCH: regla absoluta; validación vía build externo, nunca modificación.
- NO se creó `DetachAsync` (S25: paridad funcional, no clonación mecánica; cierre a 0/0).
- Comentarios de implementación actualizados SOLO donde el texto "conservado temporalmente" era explícitamente incorrecto — ahora reflejan la decisión formal S31 (RETAIN).
- `CBP.Data.Abstractions` y contratos S25 sin cambios.

## 7. Cierre

- Backups restaurables en `...\s31.1-backup-20260815-161556\` (hash verificado).
- **S31.0** = CLOSED/DISCOVERY+DESIGN ✅
- **S31.1** = CLOSED/GATE PASS ✅ (2026-08-15)
- **S32 candidato** = Observability Application (DEUDA-009 catches silenciosos `DispConfiableService.cs:83,90,121,143` + DEUDA-008 reflection `BackgroundStatusService.cs:74-82`).
