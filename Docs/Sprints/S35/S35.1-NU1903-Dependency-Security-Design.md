# S35.1 — NU1903 Dependency Security (Design)

- **Tipo**: N2 · Design (contrato de cambio congelado, matriz de gates, STOP conditions)
- **Fuente**: S35.0 Discovery (cadena transitiva + compatibilidad reproducible) · S29.2 (patrón backup/rollback)
- **Depende**: S35.0 CLOSED · S34.0 CLOSED · baseline 179/179
- **Estado**: ⏳ **DESIGN** (contrato congelado) · S35.2 = ⛔ NOT AUTHORIZED · S35.3 = ⛔ NOT AUTHORIZED
- **Modo**: READ-ONLY sobre CBP/PassPlat — 0 cambios en este sprint. Nada se implementa aquí.

---

## 1. Objetivo

Resolver **NU1903** (System.Security.Cryptography.Xml 10.0.6, severidad alta, 5 GHSA) mediante **referencia directa** a la versión patched **10.0.10** en `CBP.Excel.csproj`.

**Criterio de éxito real (gate obligatorio)**: **0 NU1903** en `CBP.slnx` Y `PassPlat.slnx`. Elevar la versión sin eliminar el warning NO es éxito — el objetivo del sprint es el build limpio de vulnerabilidad, no el bump en sí.

## 2. Contrato de cambio (congelado)

### 2.1 TOUCH (único archivo)

```
CBP/CBP.Infraestructure/CBP.Excel/CBP.Excel.csproj
    ItemGroup existente → +1 línea:
    <PackageReference Include="System.Security.Cryptography.Xml" Version="10.0.10" />
```

Composición final esperada del csproj (única diferencia: la línea añadida junto a EPPlus):

```xml
  <ItemGroup>
    <PackageReference Include="EPPlus" Version="8.5.3" />
    <PackageReference Include="System.Security.Cryptography.Xml" Version="10.0.10" />
  </ItemGroup>
```

### 2.2 NO-TOUCH (prohibido)

| Elemento | Razón |
|----------|-------|
| PassPlat productivo (`PassPlat.WebAPI` y resto) | Sin referencia a CBP.Excel; NU1903 solo vía Arch.Test transitivo |
| `PassPlat.CBP.Architecture.Test.csproj` | Se propaga automáticamente vía ProjectReference, no se edita |
| InventaNet (`Inventa.csproj`) | EPPlus directo + net10.0-windows satisface vía WindowsDesktop; 0 impacto |
| CPM (Directory.Packages.props) | Fix = pin directo, no centralización (STOP #1) |
| F8 (FrameworkReference) | Deuda diferida RETAIN |
| Analyzer Roslyn | Deuda diferida RETAIN |
| DEUDA-011 / DEUDA-012 | Deudas diferidas |
| **EPPlus 8.5.3** | NO se toca — el pin de Crypto.Xml es aditivo, no reemplazo |

## 3. Fundamentación (referencias a S35.0)

- Cadena: `CBP.Excel → EPPlus 8.5.3 → System.Security.Cryptography.Xml 10.0.6` (mínimo, sin techo; `epplus.nuspec` net10.0). 0 refs directas.
- Compatibilidad reproducible: scratch restore+build+run OK; `SignedXml` + `ExcelPackage` operativos; el pin eleva Pkcs a 10.0.10.
- Blast radius: CBP.Excel (productor) + Arch.Test (consumidor transitivo). Nada más.

## 4. Orden de operaciones S35.2 (secuencia obligatoria)

| # | Operación | Verificación | Gate |
|---|-----------|--------------|------|
| 0 | **Backup** `CBP.Excel.csproj` → `%TEMP%\opencode\s35.2-backup\` con **SHA256** previo | Backup presente + hash registrado | G0 |
| 1 | Editar csproj: +1 PackageReference 10.0.10 | Diff = exactamente 1 línea | G1 |
| 2 | **Restore remoto** de CBP.Excel | `project.assets.json`: `System.Security.Cryptography.Xml` = **10.0.10** | G2 |
| 3 | Anexo G2: Pkcs elevado | assets: `System.Security.Cryptography.Pkcs` = **10.0.10** | G2b |
| 4 | Build `CBP.slnx` | 0 errores · **0 NU1903** | G3 |
| 5 | Build `PassPlat.slnx` | 0 errores · **0 NU1903** | G4 |
| 6 | Tests full `dotnet test PassPlat.slnx --no-build` | **179/179** sin reducción · 0 errores | G5 |
| 7 | Smoke Excel/SignedXml (scratch idéntico a S35.0 o test Arch) | `ExcelPackage` funciona + `SignedXml` carga 10.0.0.0 | G6 |
| 8 | **R-01 doc fix**: `S32.2-Certification.md:186` → S19-Fx **RESUELTO** | Texto corregido (evidencia S34.0) | G7 |
| 9 | Certificación S35.2 + cierre | Deliverable + memoria | G8 |

## 5. Matriz de validación (gates formales)

| Gate | Criterio | Descripción |
|------|----------|-------------|
| **G0** | Backup SHA256 | CSRPOJ previo con hash — patrón S29.2 |
| **G1** | Diff acotado | Única línea añadida al ItemGroup |
| **G2** | Assets Xml=10.0.10 | Resolución real coincide con scratch |
| **G2b** | Assets Pkcs=10.0.10 | Efecto colateral esperado (coherente) |
| **G3** | CBP.slnx 0 errores + 0 NU1903 | **Objetivo del sprint cumplido en CBP** |
| **G4** | PassPlat.slnx 0 errores + 0 NU1903 | **Objetivo del sprint cumplido en PassPlat** |
| **G5** | 179/179 sin reducción | Baseline intacto |
| **G6** | Smoke Excel/SignedXml | Funcionalidad EPPlus intacta en runtime |
| **G7** | R-01 corregido | Doc S32.2:186 S19-Fx = RESUELTO |
| **G8** | Certificación emitida | Deliverable + supermemory |

**G3 y G4 (0 NU1903) son PUERTA DE ÉXITO**: si el build queda sin errores pero aún con NU1903, el sprint NO se cierra como PASS — se investiga.

## 6. STOP conditions (obligatorias)

1. **No mezclar CPM** — el fix es un pin directo.
2. **No tocar PassPlat** salvo regresión derivada del build.
3. **No modificar** F8, Analyzer, InventaNet ni deudas diferidas (CPM P2, DEUDA-011/012).
4. **Baseline 179/179** como referencia obligatoria al cierre.
5. Confirmar SIEMPRE cadena transitiva y versión patched antes de tocar el csproj (ya verificado en S35.0).
6. La referencia directa ≥10.0.10 debe estar demostrada compatible (validado S35.0 §5).
7. **R-01** incluido como parte del alcance documental S35.2 (G7).
8. **STOP condition adicional (usuario, S35.1)**: si el **restore real produce una resolución DISTINTA** a la demostrada en scratch (p. ej. assets ≠ 10.0.10, conflicto de grafo, versión no disponible), el sprint **se detiene** y el Design se **actualiza antes de modificar nada más**. Prohibido "forzar" la resolución por fuera.

## 7. Rollback

- **Mecanismo**: restaurar el csproj original desde `s35.2-backup\` (contenido íntegro + SHA256 verificado), `dotnet restore`, re-build.
- **Detonantes**: fail en G2 (resolución diferente), regresión en G6 (smoke), fallos build inexplicables tras el cambio.
- **Reversibilidad**: total — el pin es una línea aditiva; eliminarla restaura el estado 10.0.6 transitivo.

## 8. Confirmaación de diseño (S35.1 DELIVERABLE)

| Ítem | Contenido congelado |
|------|---------------------|
| Archivo único | `CBP/CBP.Infraestructure/CBP.Excel/CBP.Excel.csproj` |
| Cambio | `+1` `<PackageReference Include="System.Security.Cryptography.Xml" Version="10.0.10" />` |
| Gates | G0–G8 (matriz §5) |
| Gate de éxito | **0 NU1903** en CBP.slnx Y PassPlat.slnx (G3/G4) |
| STOP additional | Resolución distinta → detener + actualizar Design |
| Rollback | Backup SHA256 (patrón S29.2) |
| R-01 | Doc S32.2:186 S19-Fx = RESUELTO (G7) |
| NO-TOUCH | PassPlat productivo · InventaNet · CPM · F8 · Analyzer · DEUDA-011/012 · EPPlus |

## 9. Gate S35.1

| Criterio | Resultado |
|----------|-----------|
| Contrato de cambio congelado | ✅ §2 |
| Matriz de validación formal (gates) | ✅ §4–§5 |
| Criterio "0 NU1903" como gate obligatorio | ✅ G3/G4 |
| STOP conditions (incl. adicional del usuario) | ✅ §6 |
| Rollback con backup SHA256 definido | ✅ §7 |
| R-01 incluido en alcance | ✅ G7 |
| 0 cambios implementados | ✅ READ-ONLY |

**S35.1 = ✅ DESIGN COMPLETE** (contrato congelado) · **S35.2 = ⛔ NOT AUTHORIZED** · **S35.3 = ⛔ NOT AUTHORIZED**

---

## Estado de la cadena de sprints (actualizado)

```
S34.0 = ✅ CLOSED / DISCOVERY COMPLETE
S35.0 = ✅ CLOSED / DISCOVERY COMPLETE
S35.1 = ✅ DESIGN COMPLETE (contrato congelado)   ← aquí estamos
S35.2 = ⛔ NOT AUTHORIZED            (Implementation)
S35.3 = ⛔ NOT AUTHORIZED            (Certification / Gate Pass)
```