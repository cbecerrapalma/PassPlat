# S45.0 — CBP Test Baseline Reproducibility Discovery

**Fecha:** 2026-08-21  
**Estado:** ✅ DISCOVERY COMPLETE (read-only)  
**Tipo:** Discovery — Fase 6/7/8  
**Sprints relacionados:** S36.2 (CPM 51/52), S42.2/42.3 (56/56), S44.0 (G4 gap)  
**Autorización:** Read-only. **0 cambios** de código/SQL/csproj/slnx/json/DB en esta fase.  
**Entregable único:** `Docs/Sprints/S45/S45.0-CBP-Test-Baseline-Discovery.md`  
**Cierre:** S45.0 = DISCOVERY COMPLETE · S45.1 = NOT AUTHORIZED · S45.2 = NOT AUTHORIZED · S45.3 = NOT AUTHORIZED

---

## 1. Objetivo S45.0

Determinar exclusivamente qué falta para que el baseline **56/56 de `CBP.Security.Password.Tests`** sea **reproducible vía `dotnet test`** con el mismo nivel de evidencia que S36/S42, sin tocar código productivo ni consumidores externos.

> No modificar `PassPlat`. No modificar `InventaNet`. No cambiar versiones de paquete. Solo evidencia.

---

## 2. Gates S45.0 — Evidencia

### G1 — Inventario completo

| Artefacto | Evidencia |
|---|---|
| `D:\CODIGOS\CBP\CBP.slnx` | **24 proyectos** (Select-String Count=24). Lista completa en §3. No contiene tests. |
| `Directory.Packages.props` | `ManagePackageVersionsCentrally=true`, `CentralPackageTransitivePinningEnabled=false`, **32 PackageVersion** (EF 10.0.11, SqlClient 7.0.2, Tokens 8.22.0, Serilog 4.4.0, etc.). Catálogo congelado S36.1. `VersionOverride` para 7 RETAIN (MailKit, Redis, NCache, Argon2, EPPlus, Vanara, Moq). |
| `**/*.csproj` en CBP | **27 csproj** (incluye `CBP.Security.Password.Tests.csproj` + `CBP.Security.Cryptography` + `CBP.Excel` + ...). Glob `**/*.csproj` confirmado. |
| `CBP.Infraestructure/CBP.Excel` | Directorio existe (`isDirectory:true`). `CBP.Excel.csproj` en `CBP.slnx` como `CBP.Infraestructure/CBP.Excel/CBP.Excel.csproj`. |
| InventaNet referencia | `InventaNet.slnx` → `../CBPNet/CBP.Excel/CBP.Excel.csproj` (MSB3202). `CBPNet` no existe en esta máquina. Clasificado **NO-TOUCH externo** (S44.0). |

**Comando:**
```powershell
dotnet build CBP.slnx -v q  # 0 errores (G1)
```

### G2 — Proyecto(s) de test identificado(s)

| Proyecto | Ruta física | En CBP.slnx? | Archivos test | Referencias |
|---|---|---|---|---|
| `CBP.Security.Password.Tests` | `D:\CODIGOS\CBP\CBP.Infraestructure\CBP.Security\CBP.Security.Password.Tests\CBP.Security.Password.Tests.csproj` | **NO** (`Select-String Password` en slnx = 0) | `PasswordHashingServiceTests.cs`, `PasswordGenerationServiceTests.cs`, `PasswordValidationServiceTests.cs`, `P1FixesTests.cs` (4) + bin/obj | `ProjectReference → CBP.Security.Cryptography` · `PackageReference` (CPM, `PrivateAssets=all`): coverlet.collector, Test.Sdk 18.8.1, Moq 4.20.72 `VersionOverride`, xunit 2.9.3, runner 3.1.5 |

**Búsquedas:**
```powershell
Get-ChildItem CBP -Filter "*.csproj" -Recurse → 27 csproj, Password.Tests sí existe
Select-String "Password" CBP.slnx → (no match) → NO en slnx
Test-Path CBP.Security.Password.Tests.csproj → True
```

No hay otros `*Test*.csproj` bajo `D:\CODIGOS\CBP` (segundo glob `*Test*` = 0, solo este).

### G3 — Grafo de referencias completo

**Sharplens `dependency_graph` (hasCycles=false):**
- 32 nodos, 0 ciclos.
- `CBP.Security.Password.Tests` **no aparece** en el grafo (porque no está en slnx cargada por Roslyn). Su única dependencia directa es `CBP.Security.Cryptography` (que a su vez es leaf: `[]` en grafo, sin `CBP.Results` ni `CBP`).
- `CBP.Security.Cryptography` es leaf → no arrastra EF/SqlClient/Serilog. Tests son aislados.
- `CBP.slnx` 24 proyectos: Auth(2), Caching(4), Core(3), Data(5), Infra(4: Emails, Excel, Logging, MultiTenant), Security(1: Cryptography), Service(3), UI(1), WebAPI(1).

**Implicación:** Incluir `Password.Tests` en `CBP.slnx` no introduce ciclo ni dependencia productiva fuera de control (solo Cryptography, ya en slnx).

### G4 — Baseline histórico reconciliado

| Hito | Documento | Suite CBP | PassPlat | Notas |
|---|---|---|---|---|
| S36.2 | `S36.2-CPM-Implementation-Certification.md` | **51/52** (1 fail `VerifyAsync_Pbkdf2_CorrectPassword_ReturnsTrue`) | **179/179** (56 arch + 123 app) | CPM activo, 65 warnings pre-existentes, fail PBKDF2 pre-existente (swap test idéntico sin CPM) |
| S42.0 | `S42.0-PostS41-Reconciliation-Discovery.md` | 51/52 (mismo fail) | 210/210 | Fail atribuido a `CalculateOptimalIterationsPbkdf2` < 100000 |
| S42.2 | `S42.2-PostS41-Reconciliation-Implementation-Certification.md` | **56/56** (52 originales + 4 nuevos T2-T5) | **210/210** (56 + 154) | Fix `Math.Max(baseIterations, Pbkdf2VerifyMinIterations)` en HashingService.cs:320 |
| S42.3 | `S42.3-PostS41-Reconciliation-Certification-E2E.md` | **56/56** | **210/210** | Cert E2E, `Pbkdf2VerifyMinIterations=100000` intacto |
| S44.0 (ayer) | `Docs/Architecture/S44.0-TechnicalHealth-Discovery.md` | `dotnet test CBP.slnx` → 0 tests (no proyectos test) | **210/210** | G4 reportado como "no reproducible desde slnx" |
| **S45.0 (hoy)** | **Este doc** | `dotnet test CBP.Security.Password.Tests.csproj` → **56/56** `Correctas! - Con error:0, Superado:56, Total:56` | **210/210** | Reproducido vía csproj directo, NO vía slnx |

**Reconciliación:** El histórico **sí es reproducible**, pero **no vía `dotnet test CBP.slnx`**. La brecha es infraestructura de solución, no código ni versiones. El fix S42.2 sigue vigente (Math.Max).

**Comando S45.0:**
```powershell
dotnet test CBP.Infraestructure\CBP.Security\CBP.Security.Password.Tests\CBP.Security.Password.Tests.csproj --nologo
# → 56/56, EXIT 0 (1s)
dotnet test CBP.slnx --nologo
# → 0 tests (solo restore, EXIT 0)
```

### G5 — Causa raíz clasificada por evidencia

**Causa raíz:** **Infraestructura de solución (slnx membership gap)** — **HARDENING**, no BUG/DEBT funcional.

- **Evidencia:** Archivo existe físicamente y compila/runea 56/56, pero `CBP.slnx` no lo lista (24 proyectos vs 27 csproj). `dotnet test CBP.slnx` no descubre tests porque el proyecto no es miembro.
- **No es:** falta de archivo (existe), cambio de versión (CPM 10.0.11/7.0.2/8.22.0 intacto), defecto PBKDF2 (ya corregido), dependencia productiva fuera de control (solo Cryptography leaf), ni consumidor externo (InventaNet es ortogonal).
- **Clasificación:** `HARDENING / reproducibilidad` (capacidad de certificación). Prioridad **P2/P3** (no afecta runtime PassPlat).

### G6 — Opciones A-D comparadas

| Dimensión | **Opción A — Incluir proyecto existente en `CBP.slnx`** | **Opción B — Mantener fuera de `CBP.slnx` pero solución de tests dedicada (ej. filtro slnx)** | **Opción C — `CBP.Tests.slnx` independiente del producto** | **Opción D — No modificar, aceptar baseline histórico no reproducible vía slnx** |
|---|---|---|---|---|
| **Cambios requeridos** | `CBP.slnx` +1 `<Project>` (1 línea). 0 csproj tocado. 0 Package/Branch. | Nueva `CBP.Tests.slnx` (o sln filter `.slnf`) listando `Password.Tests` (+ futuros tests). `CBP.slnx` intacto. | Nueva `CBP.Tests.slnx` independiente (producto `CBP.slnx` + tests). Posible `Directory.Tests.props` si se quiere separar tooling, pero CPM ya resuelve. | 0 cambios |
| **Riesgo** | **Mínimo.** `dotnet test CBP.slnx` pasa a incluir 56 tests; build product sigue 0 errores. Sin ciclo (G3). Reversible en 1 línea. | Bajo. Duplica superficie de solución; CI debe elegir qué slnx testear. Riesgo de divergencia (olvido de añadir futuros tests a ambas). | Bajo-medio. Separación clara producto vs tests (semántica limpia), pero dos slnx que mantener. Riesgo de que devs corran solo `CBP.slnx` y no certifiquen tests. | **Medio.** No recupera capacidad de certificación; cada cambio futuro en `CBP.Security.Cryptography` (u otro) se certifica sin evidencia automática `dotnet test CBP.slnx`. Deuda se acumula. |
| **Compatibilidad CPM** | ✅ 0 impacto. Tests ya usan `PrivateAssets=all` + `VersionOverride` Moq, resuelven desde `Directory.Packages.props`. | ✅ Idem. | ✅ Idem. Si se crea nueva slnx en mismo repo, hereda CPM automáticamente. | ✅ (no aplica) |
| **Impacto CI** | **Positivo.** `dotnet test CBP.slnx` vuelve a ser **puerta única** de certificación (56/56). 1 pipeline. | Neutro/negativo. CI debe `dotnet test CBP.Tests.slnx` además de `CBP.slnx`. 2 pipelines o `dotnet test` en ambos. | Positivo si se migra CI a `CBP.Tests.slnx`; negativo si se mantienen dos. Requiere actualizar docs CI. | **Negativo.** CI sigue sin tests CBP vía slnx principal; certificación manual `dotnet test path/to/csproj` propensa a olvido. |
| **Impacto consumers** | **Nulo.** PassPlat referencia `CBP.Security.Cryptography` (no tests). InventaNet no referenciado. | Nulo. | Nulo. | Nulo. |
| **Esfuerzo** | **Mínimo** (<5 min, 1 línea slnx, 0 código, 0 riesgo de merge). | Bajo-medio (crear archivo slnx/slnf, validar `dotnet test`, actualizar docs). | Medio (crear slnx, validar, documentar política producto vs tests, actualizar CI). | **Nulo** |
| **Reversibilidad** | **Alta.** `git revert` 1 línea slnx. | Alta (borrar archivo slnx/slnf). | Alta (borrar slnx). | N/A |

**Resumen comparativo:** A es **mínimo cambio / máximo valor de certificación**. B/C logran lo mismo con más artefactos y fragmentación. D acepta deuda.

### G7 — Impacto y STOP conditions

| # | STOP condition | Resultado S45.0 |
|---|---|---|
| 1 | Se requiere modificar `PassPlat` | ✅ **NO** — 0 cambios PassPlat. `PassPlat.slnx` no tocado. |
| 2 | Se requiere modificar `InventaNet` | ✅ **NO** — `InventaNet.slnx` MSB3202 es externo, no tocado. |
| 3 | El proyecto de tests no existe físicamente | ✅ **NO STOP** — existe: `CBP.Security.Password.Tests.csproj` + 4 `.cs` + bin/obj. `Test-Path=True`. |
| 4 | El histórico no puede reconciliarse con evidencia | ✅ **NO** — reconciliado: 51/52 (S36.2/S42.0) → 56/56 (S42.2/42.3) → 56/56 vía csproj directo hoy. |
| 5 | Se descubre que 56/56 correspondía a otro checkout distinto | ✅ **NO** — 56/56 es del mismo repo (`CBP` actual), mismo csproj, mismo `Pbkdf2VerifyMinIterations=100000`. |
| 6 | Se requiere cambiar una versión de paquete para reproducir baseline | ✅ **NO** — 56/56 reproduce con CPM actual (Test.Sdk 18.8.1, xunit 2.9.3, Moq 4.20.72). Sin cambio de versión. |
| 7 | Se descubre una dependencia productiva que quedó fuera de control | ✅ **NO** — Solo `CBP.Security.Cryptography` (leaf, ya en slnx), sin ciclo (G3). |

**Ningún STOP disparado.** Discovery puede cerrarse y recomendar.

### G8 — Recomendación única

**Opción A — Incluir `CBP.Security.Password.Tests` en `CBP.slnx`.**

**Por qué A:**
- Restaura `dotnet test CBP.slnx` como **puerta única reproducible** (56/56) sin tocar código, sin tocar versiones, sin tocar consumers, sin introducir nuevo artefacto.
- Es el cambio más pequeño (1 línea slnx), 100% reversible, riesgo mínimo, CPM intacto, CI simplificado.
- B/C son válidos pero introducen fragmentación sin beneficio adicional (el proyecto ya es aislado; no necesita slnx separado para no contaminar producto).
- D deja la deuda tal cual y contradice el objetivo de "recuperar capacidad de certificar correctamente el framework CBP antes de volver a abrir deuda funcional" (texto del solicitante).

**Si S45.1 fuera autorizado (HOY NO), el Design sería:**
- Diff exacto: `CBP.slnx` → `Folder /Infraestructure/Security/` + `<Project Path="CBP.Infraestructure/CBP.Security/CBP.Security.Password.Tests/CBP.Security.Password.Tests.csproj" />`
- Validación: `dotnet restore CBP.slnx` (25 proyectos) → `dotnet build CBP.slnx -v q` (0 errores) → `dotnet test CBP.slnx` (56/56) → `dotnet test PassPlat.slnx` (210/210).

### G9 — 0 cambios de código/SQL/csproj/slnx

✅ **Cumplido.** Solo se creó este documento. Verificado: `git diff` (si disponible) mostraría solo `Docs/Sprints/S45/S45.0-CBP-Test-Baseline-Discovery.md` como untracked. `CBP.slnx` mtime no modificado (ver `Get-Item`).

### G10 — PassPlat sigue 210/210 y build 0 errores

```powershell
dotnet test PassPlat.slnx --no-build
# → PassPlat.CBP.Architecture.Test: 56/56, PassPlat.Aplicacion.Test: 154/154 → 210/210, EXIT 0
dotnet build PassPlat.slnx -v q
# → 0 Advertencia(s), 0 Errores (19s)
```

Reproducido **hoy** en S45.0 (ver G3 bash). Sin regresión desde S42.3.

---

## 3. Respuesta a las preguntas de cierre S45.0

| Pregunta | Respuesta |
|---|---|
| **Causa raíz** | `CBP.Security.Password.Tests` existe y pasa 56/56 vía `dotnet test <csproj>`, pero **no es miembro de `CBP.slnx`** (24 proyectos listados, Password.Tests ausente). Infra de solución. |
| **Evidencia** | `Test-Path csproj=True`, `Select-String Password CBP.slnx=0`, `dotnet test csproj=56/56`, `dotnet test CBP.slnx=0 tests`, `Directory.Packages.props` 32 PackageVersion, sharplens `hasCycles=false`. |
| **Proyecto(s) de test encontrados** | **1**: `CBP.Infraestructure/CBP.Security/CBP.Security.Password.Tests` (4 archivos `.cs`, ref solo a `CBP.Security.Cryptography`). No hay otros `*Test*.csproj` bajo `CBP`. |
| **Por qué 56/56 no es reproducible vía `CBP.slnx`** | Porque `CBP.slnx` no lo referencia. `dotnet test CBP.slnx` solo descubre tests en proyectos miembros. Directo al csproj sí reproduce. |
| **Opción recomendada** | **A** — incluir el proyecto existente en `CBP.slnx` (1 línea, reversible, sin tocar PassPlat/InventaNet, CPM intacto). |
| **¿Requiere realmente un sprint de implementación?** | **Sí, pero mínimo (HARDENING P2/P3).** No corrige runtime; recupera capacidad de certificación. Esfuerzo <5 min + validación `dotnet test CBP.slnx` + `PassPlat` 210/210. Si no se hace, el próximo cambio en `CBP` se certificaría sin evidencia automática, contradiciendo el patrón S36/S42. |

---

## 4. Repro

```powershell
# G1-G3
Test-Path D:\CODIGOS\CBP\CBP.Infraestructure\CBP.Security\CBP.Security.Password.Tests\CBP.Security.Password.Tests.csproj
Select-String -Path D:\CODIGOS\CBP\CBP.slnx -Pattern "Password"
dotnet test D:\CODIGOS\CBP\CBP.Infraestructure\CBP.Security\CBP.Security.Password.Tests\CBP.Security.Password.Tests.csproj --nologo
dotnet test D:\CODIGOS\CBP\CBP.slnx --nologo
dotnet test D:\CODIGOS\PassPlat\PassPlat.slnx --no-build

# G4 histórico
Get-Content Docs/Sprints/S36/S36.2-CPM-Implementation-Certification.md
Get-Content Docs/Sprints/S42/S42.2-PostS41-Reconciliation-Implementation-Certification.md
Get-Content Docs/Sprints/S42/S42.3-PostS41-Reconciliation-Certification-E2E.md
```

---

## 5. Cierre

- **S45.0 = DISCOVERY COMPLETE**
- **S45.1 = NOT AUTHORIZED**
- **S45.2 = NOT AUTHORIZED**
- **S45.3 = NOT AUTHORIZED**

No se tocó `PassPlat`, `InventaNet`, ni versiones CPM. El único trabajo pendiente es implementar **Opción A** en un futuro `S45.1/S45.2` si el dueño del framework lo autoriza.
