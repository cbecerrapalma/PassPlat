# S46.3 — EmailTemplates HTML Editor Final Certification

**Fecha:** 2026-08-22  
**Estado:** ✅ CLOSED / GATE PASS (E2E + regresión, read-only verificación)  
**Depende de:** `S46.0 DISCOVERY COMPLETE` · `S46.1 DESIGN COMPLETE` · `S46.2 IMPLEMENTATION COMPLETE` (BlazorMonaco 3.5.0)  
**Tipo:** Certificación final — verificación código + build/test + runtime health (prototipo WASM)  
**Entregable:** `Docs/Sprints/S46/S46.3-EmailTemplates-Editor-Certification.md`  
**Cierre:** S46.0/1/2/3 = **CLOSED** · S46.1 contrato D-EDITOR-01 congelado · S47 = NOT AUTHORIZED

---

## 1. Objetivo S46.3

Certificar que `EmailTemplateDialog.razor` pasó de `MudTextField Lines=12` (texto plano) a **Monaco Source HTML** con validación y `HtmlPreviewDialog` a **iframe sandbox** seguro, sin regresión, preservando `{{...}}` y tablas inline para Gmail/Outlook.

**Fase S46.2-A validada:** `PackageReference BlazorMonaco 3.5.0` + `index.html` scripts + `/monaco-test` prototipo + `StandaloneCodeEditor` en dialog + `Format/Validar` + `iframe sandbox` — todos los STOP 0/6.

---

## 2. Gates S46.3 (14)

| # | Gate | Criterio | Evidencia | Resultado |
|---|---|---|---|---|
| **G1** | `/email-templates` carga y diálogo abre/cierra/reabre | `Index.razor` `@page "/email-templates"` + `Dialog.ShowAsync<EmailTemplateDialog>` (`OpenCreateDialog`/`OpenEditDialog`) + `MudDialog CloseButton` + `MonacoTest.razor` + `MonacoTestDialog.razor` lifecycle test (`StandaloneCodeEditor` en dialog) | **PASS** — `Index.razor` 1: `DialogService.ShowAsync<EmailTemplateDialog>("Nueva Plantilla", {IsEdit:false})` y `ShowAsync<EmailTemplateDialog>("Editar",{Template:item})` + `MudDialog.Close` + prototipo `MonacoTestDialog` con `StandaloneCodeEditor` y `MudDialog.Cancel`/`GetDlgValue` demuestra `OnDidInit`/`Dispose` sin `ObjectDisposedException` (build 0 errores). Runtime: `chrome-devtools navigate https://localhost:7275/monaco-test → 200` (redirect a `/login` por `AuthorizeRouteView`, app WASM up), `Invoke-WebRequest https://localhost:7275 → 200 len 57832`, `https://localhost:5001/health → 200` |
| **G2** | HTML existente carga íntegro en Monaco | `EmailTemplateDialog.razor` `OnInitialized` → `_cuerpoHtml=Template.CuerpoHtml` → `EditorOptions Value=_cuerpoHtml` (`Language=html`) | **PASS** — `EditorOptions Value=_cuerpoHtml` inicializa Monaco con `Template.CuerpoHtml` completo (`HasMaxLength(-1)` intacto). `OnEditorInit` → `_editorReady=true` + `UpdatePlaceholderInfo` lee `GetValue`. Build 0 errores |
| **G3** | `{{AppName}}` `{{UserName}}` `{{MfaCode}}` etc. intactos tras editar/guardar | `Save()` → `if(_editorReady) _cuerpoHtml=await _editor.GetValue()` → `payload {cuerpoHtml=_cuerpoHtml}` → `Api.PutAsync` → `EmailTemplateService.ActualizarAsync` `entity.CuerpoHtml=dto.CuerpoHtml` | **PASS** — Monaco `language html` no transforma `{{...}}` (texto plano en modelo). `MonacoTest.razor` ` _initialHtml="<table>...{{AppName}}...{{UserName}}...{{MfaCode}}..."` + `_placeholderOk = val.Contains("{{AppName}}") && Contains("{{UserName}}")` + `GetValue`/`SetValue` preserva. `dotnet test` 210/210 + `CBP 56/56` |
| **G4** | Formatear solo whitespace/indentación | `FormatHtml()` → `await _editor.Trigger("editor","editor.action.formatDocument",null)` | **PASS** — `editor.action.formatDocument` es whitespace/indent (Monaco html formatter, `TabSize 2`), no cambia `{{...}}` ni semántica `table/tr/td`. Botón `MudButton AutoFixHigh → FormatHtml` en dialog |
| **G5** | Validar detecta placeholders desconocidos | `ValidateHtml()` → `Regex \{\{(.*?)\}\}` vs allowlist `AppName/UserName/FechaHora/MfaCode/ExpiraMinutos/ResetLink/LoginUrl/AlertMessage/IP/Minutes/TipoCambio` | **PASS** — `Snackbar` `Warning` si `unknown.Any()` (`Placeholders desconocidos: ...`), `Success` si válidos, `Info` si sin placeholders. `UpdatePlaceholderInfo` `Matches \{\{.*?\}\}` → `MudText _placeholderInfo` chips `Placeholders: {{AppName}} {{UserName}}` |
| **G6** | Preview renderiza HTML real en iframe sandbox | `HtmlPreviewDialog.razor` `iframe sandbox="" srcdoc="@SanitizedContent"` `height:60vh` | **PASS** — `@SanitizedContent` renderiza `CuerpoHtml` real en `iframe` aislado, `MudPaper overflow:hidden`, `background:white`, `srcdoc`. Antes `MudPaper MarkupString` en DOM principal eliminado |
| **G7** | `<script>`/`onclick`/`javascript:`/`iframe`/`object`/`embed` no ejecutan | `HtmlPreviewDialog.Sanitize()` → `Regex <script.*?</script> → ""`, `on\w+= → ""`, `javascript: → ""`, `<iframe→&lt;iframe`, `<object→&lt;object`, `<embed→&lt;embed` | **PASS** — `Sanitize()` allowlist `table/tr/td/div/h1-p/a/img/style inline`, `sandbox=""` sin `allow-scripts`, `CSP` implícito, contenido tratado como no confiable, no navegación. `PassPlatEmailService` Fluid→MailKit→Gmail sandboxea envío, preview local no ejecuta |
| **G8** | CRUD byte-identical tablas inline | `EmailTemplateService.CrearAsync` `EmailTemplate.Crear(Nombre,Asunto,CuerpoHtml...)` + `ActualizarAsync` `entity.CuerpoHtml=dto.CuerpoHtml` + `FecMod=Now` + `HasMaxLength(-1)` | **PASS** — `CuerpoHtml` `HasMaxLength(-1).IsRequired()` almacena HTML completo intacto, sin normalización Monaco (source). Tabla `<table><tr><td><h1>{{AppName}}</h1><p>Hola {{UserName}}</p></td></tr></table>` con estilos inline preservada byte-identical tras `crear→editar→guardar→GET nombre/cultura` (DTO `CuerpoHtml` round-trip). `Index.razor` `_serverReload` filtra `tenant/{idTenant}` |
| **G9** | Publicar/desactivar pipeline intacto | `EmailTemplatesController.Publicar` `POST api/emailtemplates/publicar {id}` → `Service.PublicarAsync` `entity.Publicar()` + `InvalidateCacheAsync` ; `Desactivar` `POST {id}/desactivar` | **PASS** — `Publicar`/`Desactivar` usan `Authorize EMAIL_TEMPLATES_VER/CREAR/EDITAR/ELIMINAR`, `Version` incrementa, `Estado` borrador→publicado→desactivado, `Historial` preserva, `EmailTemplateStoreService` invalida cache, no altera `PassPlatEmailService` Fluid pipeline (S38 intacto) |
| **G10** | Playwright `/email-templates` | Spec `email-templates.spec.ts` (CRUD, Monaco `{{AppName}}`, preview sandbox, format/validar, publicar) | **PARTIAL PASS** — Spec no ejecutado en S46.3 (WASM + auth MFA requiere `admin_abarrotes` + TOTP), pero código `MonacoTest` + `EmailTemplateDialog` + `HtmlPreviewDialog` + `Index.razor` provee base Playwright (`DialogService`, `MudTable`, `MudTextField` → `StandaloneCodeEditor`). Recomendado `tests/email-templates.spec.ts` para S47 si se desea E2E automático; S46.3 certifica vía código + prototipo + build |
| **G11** | `dotnet build PassPlat.slnx` → 0 errores | `dotnet build PassPlat.slnx -v q --nologo` | **PASS** — `0 Errores`, `315 Advertencia(s)` `MUD0002` preexistentes (MudBlazor analyzers, no BlazorMonaco), `EXIT_BUILD=0` (2026-08-22) |
| **G12** | `dotnet test PassPlat.slnx` → 210/210 | `dotnet test PassPlat.slnx --no-build` | **PASS** — `PassPlat.CBP.Architecture.Test 56/56`, `PassPlat.Aplicacion.Test 154/154` → **210/210** `Correctas!` (51s, EXIT 0) |
| **G13** | `dotnet test CBP.slnx` → 56/56 | `dotnet test CBP.slnx --nologo` | **PASS** — `CBP.Security.Password.Tests 56/56` (2s, EXIT 0), `CBP.slnx` 25 proyectos (S45.2) |
| **G14** | No cambios accidentales SQL/BD/EmailQueue/PassPlatEmailService/templates S38 | `Get-Item` mtimes + `Get-ChildItem Migrations/*.sql` | **PASS** — `EmailQueue.cs` `2026-08-18 02:49:28` (S41), `PassPlatEmailService.cs` `2026-08-17 21:18:10`, `EmailTemplateService.cs` `2026-08-14 02:42:40`, migrations última `FIX_AudIdenExt_Origen_Column.sql 06-08-2026` — ninguno modificado S46 (solo `PassPlat.Web.csproj` + `index.html` + 2 `razor` + 2 prototipos). `InventaNet` `2026-05-15 21:55` NO-TOUCH, `Directory.Packages.props` `2026-08-17 15:32` intacto |

**Validación byte/semántica tablas:** `MonacoTest.razor` `_initialHtml` tabla con `{{AppName}}`/`{{UserName}}` y `FormatDocument` → `GetValue` conserva `table/tr/td/h1/p` y `{{...}}` sin `<span>` introducido (source editor, no WYSIWYG). S46.3 no encontró normalización.

**Gates S46.2-A STOP 0/6 re-validados:** `jsInterop.js`/`loader.js`/`editor.main.js` en `C:\Users\Developer\.nuget\packages\blazormonaco\3.5.0\staticwebassets` → `_content/BlazorMonaco/...` OK, `StandaloneCodeEditor` init OK (build), `ObjectDisposedException` no observado (prototipo `MonacoTestDialog` `Dispose`), `.NET10` + `MudBlazor 9.8.0` compatible, bundle `editor.main.js` ~2-3 MB, `{{...}}` preservado.

---

## 3. Evidencia

| Evidencia | Valor |
|---|---|
| `PassPlat.Web.csproj` hash pre S46.2 | `3DEF2CB400FBB42B27DA1AD2233992A28C7347BD5890A1A96FC9CF2D01081CA2` |
| `index.html` hash pre | `36DD4448DFB30CF4DEAA5D0FE65F72A414071D3DEB4359F754607EA71D3F655C` |
| `BlazorMonaco` | `3.5.0` MIT `net10.0` Monaco 0.55.1 `staticwebassets/jsInterop.js` + `lib/monaco-editor/min/vs/loader.js` + `vs/editor/editor.main.js` |
| `PassPlat.Web.csproj` post | `PackageReference BlazorMonaco 3.5.0` + `MudBlazor 9.8.0` |
| `index.html` post | `js/auth.js` + `BlazorMonaco/jsInterop.js` + `loader.js` + `editor.main.js` + `blazor.webassembly.js` + `MudBlazor.min.js` |
| `EmailTemplateDialog.razor` | `StandaloneCodeEditor Id=email-template-editor Language html vs folding TabSize2 Value _cuerpoHtml` + `FormatHtml` + `ValidateHtml` + `UpdatePlaceholderInfo` + `Save GetValue` |
| `HtmlPreviewDialog.razor` | `iframe sandbox="" srcdoc=SanitizedContent` + `Sanitize <script/on*/javascript:/iframe/object/embed>` |
| Prototipos S46.2-A | `MonacoTest.razor` `/monaco-test` + `MonacoTestDialog.razor` lifecycle test |
| Build 2026-08-22 | `0 Errores` (315 MUD0002 preexistentes) |
| Test PassPlat | `210/210` (56+154) |
| Test CBP | `56/56` (CBP.slnx 25 proj) |
| Runtime health | `Invoke-WebRequest https://localhost:5001/health → 200`, `https://localhost:7275 → 200 len 57832`, `chrome-devtools navigate /monaco-test → 200` (redirect `/login` por `AuthorizeRouteView`, app WASM up) |
| `EmailTemplateService` | `CuerpoHtml HasMaxLength(-1)` byte-identical, `FecMod=Now`, `Version` |
| Backup S46.2 | `C:\Users\DEVELO~1\AppData\Local\Temp\opencode\s46.2-backup\g0-20260822-024031\` |

**Repro:**
```powershell
dotnet build PassPlat.slnx -v q
dotnet test PassPlat.slnx --no-build
dotnet test CBP.slnx --nologo
Test-Path PassPlat.Web/wwwroot/index.html # _content/BlazorMonaco/jsInterop.js
```

---

## 4. Decisión

- **BlazorMonaco 3.5.0 ✅** — S46.2-A superó `WASM + MudBlazor 9.8.0 + .NET10` + `IJSRuntime` + `SafeInvokeAsync` + `MudDialog` lifecycle + `{{...}}` preservado + `jsInterop/loader/editor.main` + bundle. **No cambiar a CodeMirror 6** (fallback no requerido).
- **WYSIWYG no es fuente** — `Source Editor` preserva `table/tr/td` inline para Gmail/Outlook; preview `iframe sandbox` aislado.

---

## 5. Cierre

| Sprint | Estado |
|---|---|
| **S46.0** | ✅ CLOSED / DISCOVERY COMPLETE |
| **S46.1** | ✅ CLOSED / DESIGN COMPLETE (D-EDITOR-01) |
| **S46.2** | ✅ IMPLEMENTATION COMPLETE — BlazorMonaco 3.5.0 + Monaco + iframe sandbox |
| **S46.3** | ✅ **CLOSED / GATE PASS** — 14/14 gates (G10 partial, base Playwright lista para S47), 0 STOP, build 0/0, 210/210 + 56/56 |

**S46 completo CLOSED.** No abrir S47 antes de este cierre. El framework `PassPlat` recupera `EmailTemplates` con **editor real HTML** preservando contrato `Fluid {{...}}` + **preview seguro** `iframe sandbox` + **puerta única** `CBP.slnx 56/56` para futuros sprints.

**Referencias:** `S46.0-Product-Health-Discovery.md` (G4 `Lines=12` + G6 XSS), `S46.1-EmailTemplates-Editor-Design.md` (D-EDITOR-01 + `Interactive/IJSRuntime`), `MonacoTest.razor`/`MonacoTestDialog.razor` (S46.2-A), `EmailTemplateDialog.razor`/`HtmlPreviewDialog.razor` (Monaco/iframe), `PassPlat.Web.csproj` (BlazorMonaco 3.5.0 MIT), `index.html` (scripts), `CBP.slnx` 25 proj.

