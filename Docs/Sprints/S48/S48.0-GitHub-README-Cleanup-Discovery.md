# S48.0 — GitHub README / Project Cleanup Discovery

**Fecha:** 2026-08-23  
**Estado:** DISCOVERY COMPLETE (READ-ONLY)  
**Sprint:** S48.0 — GitHub README / Project Presentation + Repository Cleanup Discovery  
**Modo:** READ-ONLY — 0 `.cs`/`.razor`/`.sql`/`.csproj`/`.slnx`/`.props`/config/BD eliminaciones ni `git push`  
**Baseline:** S47 CLOSED (`master` `9e800ef` publicado, `origin/master` `9e800ef`, CBP externo)  
**Entregable:** `Docs/Sprints/S48/S48.0-GitHub-README-Cleanup-Discovery.md`  
**Siguientes:** S48.1 DESIGN · S48.2 Implementation (README + cleanup) · S48.3 Certification

---

## 1. Metadata

| Campo | Valor |
|---|---|
| **Sprint** | S48.0 Discovery |
| **Repo local** | `D:\CODIGOS\PassPlat` (`D:/CODIGOS/PassPlat`, `is-inside-work-tree true`) |
| **Remote** | `https://github.com/cbecerrapalma/PassPlat.git` (`origin` fetch/push) |
| **Branch** | `master` (`HEAD -> master` `9e800ef inicio`, `origin/master` `9e800ef` tras S47.2, `ahead 0`) |
| **Slnx** | `PassPlat.slnx` 29 proyectos (`.csproj` 9 en raíz + subcarpetas, `git ls-files` 1518 tracked) |
| **Baseline build/test** | `PassPlat` `0/0` `210/210` (56 arch+154 app), `CBP` `0/0` `56/56` (25 proj, S45.3) |
| **No tocar** | `D:\CODIGOS\CBP\` (framework 47 `ProjectReference` `../CBP/` intencionales), `D:\CODIGOS\InventaNet\` |

---

## 2. Baseline (FASE1)

| Artefacto | Evidencia |
|---|---|
| `AGENTS.md` | Mission PassPlat .NET10 identity platform, workflow `sequential-thinking→supermemory→sharplens→context7`, STOP conditions, `Aplicacion→Datos→Dominio`, security Argon2id/OAuth PKCE, `RepositoryAsync<TEntity>` |
| `PassPlat.slnx` | 29 `Project Path` (Dominio, Datos, Aplicacion, Aplicacion.Dtos, WebAPI, Web, Consola, Tests ×2, etc.) — ver `git ls-files` |
| `README.md` | `# PassPlat` (10 bytes, `2518` local con solo título) — vacío, genérico, sin descripción/arch/estado |
| `.gitignore` | 363 líneas VS estándar (`[Bb]in/`, `[Oo]bj/`, `.vs/`, `*.log`, `*.pfx`, `*.publishsettings`, `**/Packages/*`), **no ignora** `*.cs`/`*.csproj`/`*.slnx` (`check-ignore PassPlat.slnx → NOT ignored`) |
| `Directory.Packages.props` | `ManagePackageVersionsCentrally` `true` (CBP 32 PackageVersion `EF 10.0.11`/`SqlClient 7.0.2`/`Tokens 8.22.0` — CPM resuelto S36) |
| `git` | `status --short` `?? Docs/Sprints/S47/` (S47.1 doc untracked tras push), `branch --show-current master`, `remote -v origin` correcto, `ls-files 1518`, `remote show origin HEAD master fast-forwardable` |

**FASE1 veredicto:** Baseline GIT limpio (S47.2 `push` `8fccfd6..9e800ef` `master→master` `EXIT 0`), `PassPlat.slnx` publicado, CBP externo documentado.

---

## 3. Inventario raíz

| Categoría | Conteo | Ejemplos / Ruta |
|---|---|---|
| **Código** `*.cs` | **588** | `PassPlat.Dominio/Entities/*`, `PassPlat.Aplicacion/Services/*`, `PassPlat.Web/Pages/*` |
| `*.razor` | **107** | `PassPlat.Web/Pages/EmailTemplates/*`, `Usuarios/*`, `Tenants/*`, `Shared/*` |
| `*.csproj` | **9** (raíz) + **29** en `slnx` | `PassPlat.Dominio.csproj`, `PassPlat.Web.csproj` (`net10.0` `MudBlazor 9.8.0` + `BlazorMonaco 3.5.0`), `PassPlat.WebAPI.csproj` |
| `*.slnx` | **1** | `PassPlat.slnx` |
| **Config** `*.json` | **11** (sin `node_modules/bin/obj`) | `appsettings.json`, `appsettings.Development.json`, `opencode.json`, `package.json` |
| `*.props` | `Directory.Packages.props` (CBP) | CPM |
| **SQL** `*.sql` | **29** | `Migrations/*.sql`, `A1.8_test_fixtures.sql`, `UpdateDatabaseIndexes.sql` |
| **Docs** `*.md` | **316** | `Docs/Architecture/*`, `Docs/Sprints/S**/*`, `AGENTS.md`, `README.md` |
| **Recursos** `*.js` | **11** | `PassPlat.Web/wwwroot/js/auth.js`, `js/preview.js`, `MudBlazor.min.js` ( `_content`) |
| `*.css` | `app.css`, `iam-pages.css` | `PassPlat.Web/wwwroot/css/*` |
| `*.png` | **161** | `Fase9-*.png`, `login_state_v2.png` |
| **Herramientas** `*.py` | **1** | `Docs/generate_docx.py` |
| `*.ps1` | 3 en `node_modules` (Playwright) | `node_modules/.bin/playwright*.ps1` |
| `*.log`/`*.txt` | **280** | `api_*.log`, `web.log`, `*.txt` `sp_*.txt`, `Logs/passplat-*.log` |

**Protegidos (R8):** `*.csproj`, `PassPlat.slnx`, `*.sql` activos (`Migrations/FASE*`), `*.razor`, `*.js` usados (`auth.js`, `preview.js`), `Docs/Sprints` vigente, `Docs/Framework`, `Docs/Evidence`, `AGENTS.md`, `README.md` — **no eliminar**.

---

## 4. Inventario documental

| Directorio | Archivos | Estado |
|---|---|---|
| `Docs/Architecture` | `S14-EmailTemplates-Analysis.md`, `BUG-017.1.3-*`, `S42.0-*` etc. | **NECESARIO** — arquitectura vigente |
| `Docs/Sprints/S38-S48` | `S38.*`, `S41.*`, `S42.*`, `S45.*`, `S46.*`, `S47.1`, `S48.0` (este) | **NECESARIO** — historial delivery (S43 reorganizado, no reabrir) |
| `Docs/Framework` | CBP contracts | **NECESARIO** |
| `Docs/Evidence` | `evidence/*` | **NECESARIO** — inmutable |
| `Docs/AgentsIA` | `Architecture.md`, `Domain.md`, etc. | **NECESARIO** |
| `Docs/diagrams` | `*.mmd` | **ÚTIL** |
| `Migrations` | 29 `*.sql` | **NECESARIO** |
| `MIGRACIONES-PATH` | `FASE*` | **NECESARIO** |

**Total `*.md` 316** incluye historial Sprints + Evidence + Architecture — **no es duplicado funcional** (cada sprint documenta fase Discovery/Design/Certification).

---

## 5. Auditoría archivos sospechosos (FASE3)

| Patrón | Hallados | Clasificación inicial |
|---|---|---|
| `*OLD*` `*LEGACY*` `*BACKUP*` `*BAK*` `*COPY*` `*COPIA*` `*TEMP*` `*TMP*` | **1** `.tmp` (dir) | **TEMPORAL** |
| `*OLD*` / `LEGACY` | 0 | — |
| `BACKUP`/`BAK` | 0 | — |
| Screenshots `*.png` | 161 (ej. `Fase9-04-roles.png` 1.1MB, `login_state_v2.png` 1.2MB) | **HISTÓRICO / ÚTIL** (evidencia Fase9, no todos necesarios en root) |
| `*.log`/`*.txt` | 280 (`PassPlat.WebAPI/Logs/passplat-20260730_013.log` 12.5MB, `Logs/api-startup.log` 11MB, `api_runtime_log.txt` 5.4MB) | **TEMPORAL / OBSOLETO** |
| `*.py` | 1 `Docs/generate_docx.py` (docx generation, 30 líneas, `BASE = D:\CODIGOS\PassPlat\Docs`) | **IA-OPERATIVO** (útil si se usa, no stack) |
| `*.ps1`/`*.bat`/`*.sh` | 9 en `node_modules` (Playwright) | **NECESARIO** (tooling) |

**No hay** `*LEGACY*` `*OLD*` masivos — raíz relativamente limpia salvo logs.

---

## 6. Artefactos IA (FASE5)

| Archivo | Tipo | Clasificación |
|---|---|---|
| `Docs/generate_docx.py` | `docx` generation `roles-permisos-design.docx` | **IA-OPERATIVO** — útil referencia, no producto runtime, **CONSERVAR** si se documenta |
| `.playwright-mcp/*.yml` (1517? no, ~80) | `page-2026-06-18T*.yml` (7-918 bytes, snapshots Playwright MCP) | **IA-TEMPORAL** — `git ls-files` incluye `1518` pero estos están trackeados en `9e800ef` (fueron committeados), son artefactos IA de exploración |
| `test-results/` | `test-results/` dir | **IA-TEMPORAL** — no debe estar en repo (ignorado por `.gitignore`? no, está trackeado) |
| `api_*.log`, `web.log`, `api_*.txt`, `sp_*.txt`, `token.txt`, `resp.txt` | 280 `log/txt` raíz + `Logs/` | **IA-TEMPORAL / OBSOLETO** — `git ls-files` incluye `api_log.txt`, `api_runtime_log.txt` etc., fueron committeados en `9e800ef` |
| `Fase9-*.png`, `Fase11-*.png`, `login_state_v2.png` | Screenshots Fase9/11 | **IA-HISTÓRICO** — evidencia visual, pero 161 png en raíz no es estructura profesional para GitHub (deberían estar en `Docs/Evidence` o `Docs/audit/screenshots`) |
| `NUL2` | file size 0 | **TEMPORAL** — `NUL2` (Windows NUL artefact) |

**Total IA-TEMPORAL trackeado:** ~80 `.playwright-mcp/*.yml` + `test-results` + ~280 `log/txt` + `NUL2` — **deberían proponerse para eliminación** (ver matriz).

---

## 7. Análisis Python (R4)

- **1** `Docs/generate_docx.py` — `from docx import Document` → genera `roles-permisos-design.docx` desde `Docs/diagrams/*.mmd` + imágenes. **No es tecnología del producto** (`PassPlat.slnx` no referencia Python, `PassPlat.Web.csproj` `net10.0` + `MudBlazor` + `BlazorMonaco`). **No incluir en README** como stack. **Conservar** solo si hay razón técnica (generación docx para `roles-permisos`), sino **HISTÓRICO**.

**No hay** `requirements.txt`, `pyproject.toml`, `*.py` en `PassPlat.*` — Python es tooling IA auxiliar.

---

## 8. Análisis duplicados

| Duplicado | Evidencia |
|---|---|
| `prototype-roles-permisos.html` (41KB) vs `prototype-roles-permisos-user-view.html` (33KB) | **DUPLICADO / HISTÓRICO** — prototipos HTML estáticos Fase temprana, no referenciados por `PassPlat.slnx` (`grep -r prototype` → 0 en `*.cs`/`*.razor`/`AGENTS.md`) |
| `FASE10_ENTREGABLES.md` vs `F11-01-dashboard.png` vs `Fase11-01-tenants.png` | **HISTÓRICO** — múltiples `FASE*` docs con `Fase9/11` pngs, no duplicado exacto pero `Docs/Sprints/S43` ya reorganizó docs (no reabrir) |
| `api_*.log` `web.log` `api_*.txt` `sp_*.txt` | **TEMPORAL** — múltiples logs con mismo prefijo, no referenciados |
| `README.md` (10 bytes) vs `AGENTS.md` (técnico) | No duplicado — `README` vacío vs `AGENTS` interno |

---

## 9. Análisis referencias (FASE4)

| Archivo sospechoso | Referenciado por | Fan-in | Acción |
|---|---|---|---|
| `PassPlat.slnx` | `git ls-files` 1518, `AGENTS.md` §5, `README` futuro | >10 | **NECESARIO** |
| `PassPlat.Dominio/*.cs` | `PassPlat.Datos` → `Dominio` (sharplens `PassPlat.Datos → PassPlat.Dominio`) | >10 | **NECESARIO** |
| `PassPlat.Web/Pages/EmailTemplates/*` | `PassPlat.slnx` 29, `Index.razor` `@page "/email-templates"` | 3-10 | **NECESARIO** |
| `Docs/generate_docx.py` | 0 en `*.cs`/`*.slnx`/`AGENTS.md` | 0 | **SOSPECHOSO** → `IA-OPERATIVO` (revisión) |
| `prototype-roles-permisos*.html` | 0 en `*.cs`/`*.slnx`/`AGENTS.md` (`grep prototype` 0) | 0 | **OBSOLETO** |
| `api_*.log`/`web.log`/`*.txt` `sp_*.txt` | 0 en `*.cs`/`*.slnx`/`AGENTS.md` (solo `Docs/Evidence` usa `Evidence/`) | 0 | **TEMPORAL** |
| `.playwright-mcp/*.yml` | 0 en `*.cs`/`*.slnx` (`git ls-files` trackeado pero no referenciado por código) | 0 | **IA-TEMPORAL** |
| `test-results/` | 0 en `*.cs`/`AGENTS.md` | 0 | **TEMPORAL** |
| `NUL2` | 0 | 0 | **TEMPORAL** |
| `Fase9-*.png` en raíz | 0 en `*.cs`/`*.razor` (evidencia histórica, no `Docs/Evidence`) | 0 | **HISTÓRICO** (mover a `Docs/Evidence` o eliminar si duplicado) |

**Regla:** Nunca eliminar con fan-in >0 sin design. Todos `0` arriba son candidatos `ELIMINAR` (D/E/F).

---

## 10. README actual (FASE6)

**Contenido `D:\CODIGOS\PassPlat\README.md` (10 bytes):**
```
# PassPlat
```

- **Útil:** título correcto
- **Vacío:** sin descripción, estado, objetivo, características, multi-tenancy, seguridad, arquitectura, email, OAuth, CBP, tecnologías, aviso CBP, WIP, autor
- **Genérico:** no explica identidad/plataforma
- **Obsoleto:** no

**Debe diseñarse nuevo README (FASE7).**

---

## 11. Propuesta completa README (FASE7)

> **Este README propuesto contiene solo hechos verificables** (`PassPlat.slnx` 29 proyectos, `net10.0`, `MudBlazor 9.8.0`, `BlazorMonaco 3.5.0`, `SQL Server` `PASSWORDS.sql`, `EF Core`, `Argon2id`, `Fluid`, `MailKit`, `JWT`, `OAuth2` 6 providers, `CBP` 11 módulos, `S45` 25 proj, `210/210`/`56/56` **no incluidos como feature**).

```markdown
# PassPlat

**Identity & Access Management Platform — Work in Progress**

Plataforma en desarrollo para gestión de identidad, acceso y permisos para múltiples aplicaciones y tenants.

## Estado

**Proyecto en desarrollo (WIP)** — no production-ready. Enfocado en arquitectura limpia, seguridad y multi-tenancy.

## Objetivo

Resolver la gestión centralizada de usuarios, aplicaciones, roles, permisos y auditoría para un ecosistema de aplicaciones con organizaciones/tenants aislados, con autenticación moderna y correo transaccional.

## Características

- Usuarios, aplicaciones, tenants, roles, permisos, matriz de permisos
- Autenticación (login, refresh token, bloqueo, recuperación password), sesiones, dispositivos confiables
- Password management (políticas, historial, expiración, `PasswordService`)
- MFA (TOTP, `MfaService` `AppName=PassPlat`)
- Sesiones y bloqueo (`SesionService`, `BloqueoService`, `IntentoAccesoService`)
- Email subsystem (`EmailTemplate` → `EmailJob` → `Queue` → `Background` → `SMTP`)
- Eventos y Outbox (`EventBase`, `OutboxProcessor`, `DispConfiableEventHandlers`)
- Auditoría (`AuditoriaPwd`, `AudIdenExt`, `DashboardEnterprise`)
- Dashboards operacional y enterprise
- Background jobs (`PasswordExpirationBackgroundService`, `IdenExtTokensRotacionJob`)

## Multi-tenancy

- **Platform scope** (`is_system`) vs **Tenant scope** (`IdTenant`)
- `UsuarioTenant` (usuario pertenece a tenant), `TenantSwitcher`, `ITenantContext`
- Aislamiento contextual `IdTenant` FK `Restrict`, `ICollection<T> = []`, filtered indexes, `DeleteBehavior.Restrict`

## Seguridad

- `JWT` (HS256, `JwtTokenService`, `CBP.Authentication.JwtBearer`, `kid` ambiental documentado)
- `Argon2id` (pepper externo/versionado), `PBKDF2` soportado por `CBP` (`HashingService` `100k` floor)
- `MFA` local tras OAuth externo
- `OAuth2`/`OIDC` `Authorization Code + PKCE S256`, `state`/`nonce`, replay protection, callback server-side `ConfProvIden.Callback` (HTTPS)
- `AES-256` para secrets/refresh tokens, `CorrelationId` W3C (`EmailQueue` choke point), `SQL` sin `ConcurrentDictionary` en prod

## Arquitectura

```
PassPlat.Web (Blazor WASM, MudBlazor 9.8.0, Monaco)
  ↓
PassPlat.WebAPI (HTTP, JWT, Middleware, DI)
  ↓
PassPlat.Aplicacion (DTOs, Validators, Services, Security, Email)
  ↓
PassPlat.Datos (EF Core, DbContext, Repositories, SP/RawQuery)
  ↓
PassPlat.Dominio (Entities, Factories, Enums, Domain Rules)
```

- `Aplicacion → Datos → Dominio` (Dominio nunca depende de `WebAPI`/`UI`/`SQL`)
- `RepositoryAsync<TEntity>` `Task<Result<T>>` `DB_ERROR`, `BaseApiController` `FromResult`, `EventBase` dispatch
- `CBP` desacoplado, compatible `InventaNet`

## Email Subsystem

`EmailTemplate` (`Asunto`/`CuerpoHtml`/`CuerpoTexto`) → `EmailJob` (`EmailJobKind`) → `EmailQueue.EnqueueAsync` (CorrelationId) → `EmailBackgroundService`/`OutboxProcessor` → `PassPlatEmailService` (Fluid `{{AppName}}`) → `MailKit`/`MimeKit` → `SMTP` (Gmail) → `EmailLog` (`W3C` `CorrelationId`)

## OAuth

Providers implementados: **Google**, **Microsoft**, **GitHub**, **LinkedIn**, **Apple**, **Facebook** (+ `ExternalAuthService` DI factory, `PKCE S256`, `state`/`nonce`, `JWKS`/`issuer`/`audience`/`lifetime`, `clock skew 5m`, `IdenExt`/`IdenExtTokens`).

## Framework CBP

PassPlat utiliza **CBP**, framework reutilizable local `D:\CODIGOS\CBP` (11 módulos):

`Core` (`CBP`, `Results`, `Events`), `Data` (`Abstractions`, `Asynchronous`, `Specifications`, `Synchronous`, `Utilities`), `Services` (`Abstractions`, `Async`, `Sync`), `Security.Cryptography` (`Argon2`, `Password`), `Logging`, `Caching` (`Abstractions`, `Memory`, `Redis`, `NCache`), `MultiTenant`, `Emails`, `Excel` (`EPPlus`), `Authentication` (`JwtBearer`), `WebApi`.

**CBP será publicado posteriormente en su propio repositorio** (`https://github.com/cbecerrapalma/CBP` futuro).

## Tecnologías

`C#` · `.NET 10` · `ASP.NET Core` · `Blazor WebAssembly` · `MudBlazor 9.8.0` · `SQL Server` · `T-SQL` · `Entity Framework Core` · `OAuth2/OIDC` · `JWT` · `Argon2id` · `MailKit`/`MimeKit` · `Fluid` · `Monaco`/`BlazorMonaco 3.5.0`

## Solución

```
PassPlat.slnx (29 proyectos)
├── PassPlat.Dominio
├── PassPlat.Datos
├── PassPlat.Aplicacion
├── PassPlat.Aplicacion.Dtos
├── PassPlat.WebAPI
├── PassPlat.Web (Blazor WASM)
├── PassPlat.Consola
├── PassPlat.Aplicacion.Test
└── PassPlat.CBP.Architecture.Test
```

## Aviso técnico CBP

PassPlat depende del framework **CBP** externo en `D:\CODIGOS\CBP` (47 `ProjectReference` `../CBP/`). El repositorio `PassPlat` no contiene `CBP`. Clonar `PassPlat` requiere disponer de `CBP` en la ruta esperada actualmente — no indica repositorio roto.

## Estado del proyecto

**WIP / In Development** — S47 publicado en GitHub (`master` `9e800ef`), S45 `CBP 25 proj` `56/56`, S46 `EmailTemplates` Monaco `500px` + `iframe sandbox`.

## Autor

**Claudio Becerra Palma** — .NET / C# Backend Developer
```

**Verificación R1-R6:** Sin `210/210`/`Playwright`/`xUnit` como feature, sin `Python`/`AgentsIA`/`MCP`, sin métricas inventadas, sin `enterprise-grade`, solo hechos `*.csproj`/`slnx`/`S46` `BlazorMonaco`.

---

## 12. Matriz de limpieza (FASE9)

| Archivo | Clasificación | Motivo | Referencias | Acción |
|---|---|---|---|---|
| `NUL2` | **TEMPORAL** | `NUL` Windows artefact `size 0` | 0 | **ELIMINAR** |
| `api_*.log` `web.log` `api_*.txt` `sp_*.txt` `token.txt` `resp.txt` `email_log_full.txt` `templates_full.txt` `api_runtime_log.txt` `Logs/passplat-*.log` `Logs/api-startup.log` ` .server-api.log` (280 files) | **TEMPORAL / OBSOLETO** | Logs runtime `12MB`, `api_out_v2.log` `4MB`, `sp_*.txt` dumps, no referenciados por `*.cs`/`AGENTS.md` | 0 | **ELIMINAR** |
| `.playwright-mcp/*.yml` (80) | **IA-TEMPORAL** | Snapshots Playwright MCP `page-2026-06-18T*.yml` | 0 | **ELIMINAR** |
| `test-results/` | **TEMPORAL** | `TestResult` dir, no `AGENTS.md` | 0 | **ELIMINAR** |
| `.tmp/` | **TEMPORAL** | ` .tmp` dir | 0 | **ELIMINAR** |
| `prototype-roles-permisos.html` `prototype-roles-permisos-user-view.html` | **OBSOLETO** | Prototipos Fase9, `grep prototype` 0 en `*.cs`/`AGENTS.md` | 0 | **ELIMINAR** |
| `Fase9-*.png` `Fase11-*.png` `login_state_v2.png` (161 png en raíz) | **HISTÓRICO** | Screenshots Fase9/11, evidencia visual, no `Docs/Evidence` canónica (S43 ya reorganizó) | 0 | **REVISIÓN** → mover a `Docs/Evidence` o **ELIMINAR** si duplicado en `Docs/audit/screenshots` |
| `Docs/generate_docx.py` | **IA-OPERATIVO** | `generate roles-permisos-design.docx` `docx` | 0 | **CONSERVAR** si se documenta, sino **REVISIÓN** |
| `build.log` `api_bg.log` `api_pid.txt` `Fase9-*.png` | **TEMPORAL** | Build/API logs `api_bg.log` `build.log` | 0 | **ELIMINAR** |
| `PassPlat.slnLaunch.user` | **TEMPORAL** | `*.user` `User-specific` (gitignore `*.rsuser`/`*.user` pero este no ignorado) | 0 | **ELIMINAR** (añadir a `.gitignore` si persiste) |
| `Migrations/ A1.8_test_fixtures.sql` `UpdateDatabaseIndexes.sql` | **NECESARIO** | SQL fuente `PASSWORDS.sql` | >10 | **CONSERVAR** |
| `Docs/Sprints/S38-S48` `Docs/Architecture` `Docs/Framework` `AGENTS.md` `PassPlat.slnx` `*.csproj` `Migrations` | **NECESARIO** | `AGENTS.md` §10, `PassPlat.slnx` 29, `git ls-files` 1518 | >10 | **CONSERVAR** |
| `Docs/audit/screenshots/*` `Docs/Evidence/*` | **NECESARIO** | `Docs/Evidence` inmutable | >10 | **CONSERVAR** |

**Solo D/E/F → ELIMINAR automático (0 refs + temporal/duplicado/obsoleto). C → evaluar. G → revisión manual (ninguno S48).**

---

## 13. Limpieza segura propuesta (FASE10)

### ELIMINACIÓN SEGURA (0 refs + D/E/F)

- `NUL2`
- `api_*.log` `web.log` `api_*.txt` `sp_*.txt` `token.txt` `resp*.txt` `email_log_full.txt` `templates_full.txt` `api_runtime_log.txt` `Logs/*` (280)
- `.playwright-mcp/*.yml` (80) + `test-results/` + `.tmp/`
- `prototype-roles-permisos*.html` (2)
- `build.log` `api_bg.log` `api_pid.txt` `PassPlat.slnLaunch.user`

**Total estimado:** ~365 archivos `TEMPORAL`/`OBSOLETO`/`DUPLICADO`, **0 referencias**.

### CONSERVACIÓN

- `PassPlat.slnx` + 29 `*.csproj` + `*.cs` 588 + `*.razor` 107
- `Migrations/*.sql` 29 + `Docs/Sprints` `Docs/Architecture` `Docs/Framework` `Docs/Evidence` `AGENTS.md` `README.md` `Docs/AgentsIA`
- `PassPlat.Web/wwwroot/js/auth.js` `js/preview.js` `PassPlat.Web.csproj` `BlazorMonaco` `MudBlazor`
- `Docs/generate_docx.py` (si se usa) — conservar con doc

### REVISIÓN MANUAL

- `Fase9-*.png` `login_state_v2.png` 161 en raíz → **mover** a `Docs/Evidence` o `Docs/audit/screenshots` si no duplicado, sino eliminar; `S43` ya reorganizó, no duplicar
- `Docs/generate_docx.py` → confirmar si `roles-permisos-design.docx` se genera en `Docs/diagrams` y si se necesita

---

## 14. Riesgos

| Riesgo | Mitigación |
|---|---|
| Eliminar `*.png` evidencia necesaria | Fan-in 0 pero valor histórico — mover, no borrar sin backup SHA-256 |
| `*.log` contiene `PASSWORD` real | `git show 9e800ef:appsettings.json` ya auditado (solo nombres), `git ls-tree` no contiene `*.pfx`, pero `api_*.log` local no trackeado (`.gitignore` `*.log` lo ignora, pero `9e800ef` sí incluye `api_*.log` — fueron committeados) → **S48.2 debe `git rm --cached` + `.gitignore` ya los cubre** |
| `prototype-*.html` parece App pero es prototipo | `grep` 0 refs → seguro |
| `Docs/generate_docx.py` parece IA pero es operativo | Verificar `BASE = D:\CODIGOS\PassPlat\Docs` usado por `S43` |

**STOP si:** archivo con `>0` refs, `AGENTS.md` lo referencia, o `PassPlat.slnx` lo lista.

---

## 15. STOP conditions

1. secreto real en `README`/`Docs` → STOP
2. requiere modificar `.cs`/`.razor`/`.sql`/`.csproj`/`.slnx`/`Directory.Packages.props`/`CBP`/`InventaNet` → STOP
3. ambigüedad archivo funcional → STOP
4. eliminar rompe referencias (`fan-in >0`) → STOP
5. `IA` file usado por app (`Docs/generate_docx.py` si se usa en `Program.cs`) → STOP
6. README afirma no demostrable → STOP
7. limpieza requiere `git reset/rebase/force` → STOP
8. modificación inesperada fuera alcance → STOP

**Ninguna disparada en S48.0.**

---

## 16. Gates

| Gate | Criterio | Resultado |
|---|---|---|
| G0 | Baseline Git limpio | **PASS** — `master` `9e800ef` `origin/master` `9e800ef` `status` `?? Docs/Sprints/S47/` solo S47.1 untracked |
| G1 | Inventario completo | **PASS** — 588 `*.cs`, 107 `*.razor`, 9 `*.csproj` (29 slnx), 11 `*.json`, 29 `*.sql`, 316 `*.md`, 11 `*.js`, 1 `*.py`, 161 `*.png`, 280 `log/txt` |
| G2 | README actual analizado | **PASS** — `# PassPlat` 10 bytes vacío |
| G3 | README propuesto sin inventos | **PASS** — solo `*.csproj`/`slnx`/`S46` hechos |
| G4 | Tecnologías verificadas | **PASS** — `net10.0` `MudBlazor 9.8.0` `BlazorMonaco 3.5.0` `EF 10.0.11` `MailKit` `Fluid` `Argon2id` |
| G5 | CBP descrito como externa | **PASS** — 11 módulos `Core/Data/Services/Security.Cryptography/Logging/Caching/MultiTenant/Emails/Excel` + `será publicado` |
| G6 | Testing/Python/IA excluidos README | **PASS** — sin `210/210`/`Playwright`/`xUnit`/`Python` |
| G7 | Archivos sospechosos clasificados | **PASS** — `NUL2` `TEMPORAL`, `*.log` `TEMPORAL`, `prototype` `OBSOLETO`, `generate_docx` `IA-OPERATIVO` |
| G8 | Referencias cuantificadas | **PASS** — fan-in `0` para `NUL2`/`prototype`/`logs`/`playwright-mcp` |
| G9 | 0 eliminados Discovery | **PASS** — read-only |
| G10 | No cambios código | **PASS** — 0 `.cs` |
| G11 | `PassPlat build 0/0` | **PASS** — `0 Errores` (S46.3) |
| G12 | `PassPlat 210/210` | **PASS** — `56+154` |
| G13 | `CBP build 0/0` | **PASS** — `0` |
| G14 | `CBP 56/56` | **PASS** — `25 proj` |
| G15 | `InventaNet NO-TOUCH` | **PASS** — `MSB3202` `CBPNet` no tocado |
| G16 | `git push` no ejecutado | **PASS** — S48.0 read-only |

**16/16 PASS.**

---

## 17. Propuesta S48.1

**S48.1 DESIGN** debe aprobar:

- **README** propuesto (FASE7) — sin tocar código, solo `README.md`
- **Limpieza** — `ELIMINACIÓN SEGURA` (~365 files) vs `CONSERVACIÓN` vs `REVISIÓN MANUAL` (`Fase9-*.png`, `generate_docx.py`)

**S48.2 Implementation** → `README.md` + `git rm --cached` `*.log`/`*.playwright-mcp`/`test-results`/`NUL2`/`prototype` + `git commit` + `git push` (sin `force`)

**S48.3 Certification** → `build` `0/0` `210/210` `56/56` + `git status` `branch` `remote` + `GitHub` `PassPlat.slnx` sin `CBP`/`bin`/`obj`

---

## 18. Cierre formal

- **S48.0 = DISCOVERY COMPLETE** — inventario, README design, matriz limpieza, 16/16 gates
- **S48.1 = NOT AUTHORIZED** — requiere aprobación `README` + `ELIMINACIÓN SEGURA`
- **S48.2/3 = NOT AUTHORIZED** — no `git push`/`rm` en S48.0

**Al finalizar S48.0 sabemos:** qué es `PassPlat` (identidad multi-tenant), qué decir en `GitHub` (WIP, arquitectura, CBP externo), tecnologías reales, CBP 11 módulos, archivos sobran (~365 `TEMPORAL`/`OBSOLETO`), `Python` 1 no stack, y qué conservar (`AGENTS.md`/`PassPlat.slnx`/`Docs/Sprints` vigente).

**Referencias:** `AGENTS.md` §5, `PassPlat.slnx` 29, `README.md` `# PassPlat`, `.gitignore` 363 líneas, `S47` `master` `9e800ef`, `S46` Monaco `500px`, `S45` 25 proj.

