# S48.1 — GitHub README / Cleanup Design

**Fecha:** 2026-08-23  
**Estado:** DESIGN COMPLETE (READ-ONLY)  
**Depende de:** `S48.0-GitHub-README-Cleanup-Discovery.md` (16/16 PASS, 588 cs/107 razor/29 slnx/11 json/29 sql/316 md)  
**Tipo:** Diseño — 0 `git rm`/`push`/`code` en esta fase  
**Entregable:** `Docs/Sprints/S48/S48.1-GitHub-README-Design.md`  
**Cierre:** S48.0 = CLOSED · S48.1 = DESIGN COMPLETE · S48.2 = NOT AUTHORIZED · S48.3 = NOT AUTHORIZED

---

## 1. Objetivo S48.1

Congelar **README profesional** y **matriz exacta de limpieza** sin bulk delete, para que `S48.2` ejecute solo `README.md` + `git rm --cached` explícito + `.gitignore` y `S48.3` certifique `build 0/0` `210/210` `56/56`.

**Correcciones a S48.0 (feedback usuario):**
- NO eliminar 280 `log/txt` en bloque — separar `temporal` / `evidencia histórica` / `sensible`
- NO mover 161 `png` automático — `HISTÓRICO→REVISIÓN` vs `Docs/Evidence` + `Docs/audit/screenshots` (134 ya en audit)
- NO `git rm --cached` genérico por patrón — eliminación explícita y auditable (público)

---

## 2. README congelado (FASE7 corregido)

**Principios R1-R9 S48.0:** solo hechos `PassPlat.slnx` 29, `net10.0`, `MudBlazor 9.8.0`, `BlazorMonaco 3.5.0`, `SQL Server` `PASSWORDS.sql`, `EF Core`, `Argon2id`/`PBKDF2` `CBP`, `Fluid` `MailKit`, `JWT` `OAuth2` 6 providers, `CBP` 11 módulos — sin `210/210`/`Playwright`/`xUnit`/`Python`/`MCP`/`enterprise-grade`.

**Contenido 16 secciones congelado** (propuesta S48.0 §11, sin afirmaciones excesivas):

1. `PassPlat` — título
2. Descripción breve — plataforma WIP identidad/usuarios/autenticación/autorización/roles/permisos/multi-app/multi-tenant
3. Estado — `Proyecto en desarrollo (WIP)` (no `production-ready`)
4. Objetivo — gestión centralizada usuarios/apps/tenants/permisos/auditoría
5. Características — usuarios/apps/tenants/roles/permisos/auth/MFA/OAuth2/sesiones/dispositivos/password/email/eventos/outbox/auditoría/dashboards (verificables `PassPlat.Aplicacion/Services/*`, `AuthService`, `MfaService`, `EmailBackgroundService`)
6. Multi-tenancy — `Platform` vs `Tenant` `UsuarioTenant` `ITenantContext` `IdTenant Restrict`
7. Seguridad — `JWT` `Argon2id` `PBKDF2` `CBP` `MFA` `OAuth2 PKCE` `state/nonce` `AES-256` `CorrelationId` (sin prometer `highly scalable`)
8. Arquitectura — `Web (Blazor WASM)` → `WebAPI` → `Aplicacion` → `Datos` → `Dominio` + `CBP` desacoplado
9. Email — `Template` → `EmailJob` → `Queue` → `Background` → `SMTP` (Fluid `{{AppName}}`)
10. OAuth — Google/Microsoft/GitHub/LinkedIn/Apple/Facebook (solo 6 implementados `IExternalIdentityProvider`)
11. CBP — `Core` `Results` `Events` `Data` `Services` `Security.Cryptography` `Logging` `Caching` `MultiTenant` `Emails` `Excel` → `será publicado posteriormente en su propio repositorio`
12. Tecnologías — `C#` `.NET10` `ASP.NET Core` `Blazor WASM` `MudBlazor 9.8.0` `SQL Server` `T-SQL` `EF Core` `OAuth2/OIDC` `JWT` `Argon2id` `MailKit/MimeKit` `Fluid` `Monaco/BlazorMonaco 3.5.0` (sin `Python` salvo evidencia, sin testing)
13. Solución — `PassPlat.slnx` 9 `csproj` raíz (Dominio/Datos/Aplicacion/Dtos/WebAPI/Web/Consola/2 Tests)
14. Aviso CBP — `PassPlat` depende `D:\CODIGOS\CBP` externo `47 ProjectReference ../CBP/` — repo no contiene `CBP`, `clone` requiere `CBP` en ruta
15. Estado — `WIP / In Development`
16. Autor — **Claudio Becerra Palma — .NET / C# Backend Developer** (sin `senior/expert/architect`)

**Corrección S48.1:** eliminar cualquier `highly scalable`/`production-ready`/`enterprise-grade`/métricas comerciales — ya excluido en S48.0 §11.

---

## 3. Matriz exacta — ELIMINACIÓN SEGURA (0 refs, D/E/F, explícita)

**Solo estos, con `git rm --cached` por ruta exacta (no `*.log` genérico):**

| Archivo (exacto) | Clasificación | Motivo |
|---|---|---|
| `NUL2` | **TEMPORAL** | `NUL` Windows artefact `0` bytes, `fan-in 0` |
| `PassPlat.slnLaunch.user` | **TEMPORAL** | `*.user` VS user-specific (`*.rsuser` en `.gitignore` pero este no) |
| `build.log` | **TEMPORAL** | `MUD0002` 315 warnings `PassPlat.Web`, no `Docs/Evidence` |
| `api_bg.log` `api_pid.txt` | **TEMPORAL** | `dotnet run` pid/log, `fan-in 0` |
| `.tmp/` | **TEMPORAL** | `dir .tmp` vacío |
| `prototype-roles-permisos.html` `prototype-roles-permisos-user-view.html` | **OBSOLETO** | `grep prototype` `0` en `*.cs`/`AGENTS.md`/`slnx`, Fase9 prototipo |
| `.playwright-mcp/` (80 `*.yml`) | **IA-TEMPORAL** | Snapshots MCP `page-2026-06-18T*.yml`, trackeados en `9e800ef` pero no referenciados por código, `Tests/` usa `playwright.config.ts` |
| `test-results/` | **TEMPORAL** | `TestResult` dir, `*.coverage` ya en `.gitignore` |
| **Total ELIMINACIÓN SEGURA** | **~87 files** | **Auditable, 0 refs, reversible via `git log 9e800ef`** |

**NO incluir en S48.2:** `280 log/txt` en bloque ni `161 png` en bloque — ver §4/5 revisión.

---

## 4. Revisión logs/txt — NO bulk (280 files)

**S48.0 propuso `ELIMINAR` 280 en bloque — corregido S48.1:**

| Subconjunto | Ejemplo | Clasificación | Acción S48.2 |
|---|---|---|---|
| **Claramente temporal** | `.playwright-mcp/*.yml` (80), `test-results/`, `.tmp/`, `build.log`, `api_bg.log`, `api_pid.txt`, `NUL2`, `prototype-*.html` | **TEMPORAL** | **ELIMINAR** (arriba) |
| **Evidencia histórica** | `Fase9-*.png` no, pero `api_*.log` `Logs/passplat-*.log` `api_runtime_log.txt` (5.4MB), `email_log_full.txt`, `sp_*.txt`, `templates_full.txt` — algunos fueron `Docs/Evidence` en S39 (`Logs/s39-api-out.log`) pero 280 en raíz son **runtime logs**, no `Docs/Evidence` canónica (S43 reorganizó) | **HISTÓRICO / REVISIÓN** | **REVISIÓN MANUAL** — verificar si alguno es `Evidence` inmutable (`Docs/Evidence/` ya contiene canónica) o duplicado `Logs/`; si es `api_out_v2.log` (4.7MB) sin referencia `AGENTS.md`, proponer `ELIMINAR` pero **listar explícito** por nombre, no `*.log` |
| **Posible sensible** | `token.txt` (2019 bytes), `api175_stdout.txt` (8019), `sp_auth_login.txt` (10549), `email_log_full.txt` (3128), `api_log.txt` (57589) — `grep Password|Secret|Token` en commit `9e800ef` solo mostró nombres `SecretKey/ConnectionStrings` en `BUG-017.1.3.md`, no valores, pero `token.txt`/**`api_*.txt`** podrían contener `JWT`/`ConnectionString` real | **SOSPECHOSO — REVISIÓN** | **NO ELIMINAR ni PUBLICAR sin `git show 9e800ef:token.txt` redacted** — S48.1 marca `REVISIÓN MANUAL`; S48.2 debe `git show` cada `*.txt`/`*.log` y reportar `archivo:línea:TIPO` sin valor, detener si `Password=P@ss` real |

**Regla S48.1:** `fan-in 0` ≠ seguro para público — añadir **sensibilidad** y **utilidad histórica** como criterios. `S48.2` hará `git rm --cached` **por ruta exacta** para cada `log/txt` aprobado, no `git rm --cached *.log`.

**Acción S48.1:** Dejar `280` en `REVISIÓN`, solo `87` de `ELIMINACIÓN SEGURA` aprobados arriba pasan a `S48.2`.

---

## 5. Revisión PNG 161 vs Docs/Evidence + `generate_docx.py`

| PNG | Ubicación | Duplicado | Acción |
|---|---|---|---|
| `Fase9-01-home.png` `Fase9-*.png` `login_state_v2.png` (12 en raíz) | Raíz | **Sí** — `Docs/audit/screenshots/` ya tiene 134 png (`00-dashboard.png`, `21-email-templates.png`, `fase12_login_page.png` etc.) y `Docs/Evidence` inmutable | **REVISIÓN** — comparar ` Fase9-10-email-templates.png` (946KB en `git ls-files`) vs `Docs/audit/screenshots/21-email-templates.png` (mismo contenido?) Si duplicado → **ELIMINAR** raíz, conservar `Docs/audit`; si único → **MOVER** a `Docs/Evidence` |
| `Docs/generate_docx.py` (1 `*.py`) | `Docs/` | `grep generate_docx` 0 en `*.cs`/`AGENTS.md`/`Sprints` | **IA-OPERATIVO / REVISIÓN** — `BASE = D:\CODIGOS\PassPlat\Docs` genera `roles-permisos-design.docx` desde `diagrams/*.mmd`; no es stack `*.csproj` (`net10.0`), **no incluir en README**, **CONSERVAR** solo si `S43` lo documenta como operativo, sino **HISTÓRICO** |

**S48.1 no mueve PNG** — deja `161` en `REVISIÓN` para `S48.2` decidir con `diff` binario.

---

## 6. Política `.gitignore`

**Actual:** 363 líneas VS estándar, `NOT ignored` `PassPlat.slnx` — correcto.

**S48.2 debe añadir (si no existe):**

```
# S48.1 — evitar re-entrada artefactos temporales
.playwright-mcp/
test-results/
.tmp/
NUL2
PassPlat.slnLaunch.user
prototype-roles-permisos*.html
```

Ya ignora `[Bb]in/` `[Oo]bj/` `.vs/` `*.log` `*.vspscc` — `api_*.log` ya cubierto, pero `*.txt` **no** está ignorado (`token.txt` etc. se trackearon en `9e800ef`) — S48.1 no añade `*.txt` genérico (podría ocultar `sp_*.txt` útiles), solo ignora temporales explícitos arriba.

---

## 7. STOP conditions S48.1

1. secreto real en `README`/propuesta → STOP
2. README afirma no demostrable (ej. `210/210` como feature) → STOP (ya excluido)
3. limpieza requiere `git reset/rebase/force` → STOP
4. eliminar rompe `fan-in >0` → STOP
5. `Docs/generate_docx.py` usado por `PassPlat.slnx` → STOP (no, `0` refs)
6. archivo `HISTÓRICO` con valor (ej. `Fase9-*.png` único) → STOP y mover a `Docs/Evidence`

**Ninguna disparada — S48.1 DESIGN COMPLETE.**

---

## 8. Gates S48.0 → S48.1

| Gate | S48.0 | S48.1 |
|---|---|---|
| G0 baseline `master` `9e800ef` `origin/master` `9e800ef` | PASS | PASS (re-verificado `status` `?? Docs/Sprints/S47/` solo S47.1 untracked) |
| G1 inventario 588/107/29 | PASS | PASS |
| G2 README `# PassPlat` vacío | PASS | **PASS** (propuesta 16 secciones) |
| G3 README sin inventos | PASS | **PASS** (solo `PassPlat.slnx` hechos) |
| G4 tecnologías verificadas | PASS | **PASS** (`MudBlazor 9.8.0` `BlazorMonaco 3.5.0` `EF 10.0.11`) |
| G5 CBP 11 módulos | PASS | **PASS** (`../CBP/` 47 refs, `será publicado`) |
| G6 sin testing/Python | PASS | **PASS** |
| G7 sospechosos clasificados | PASS (280 bulk) | **CORREGIDO** → `87` SEGURA + `280` REVISIÓN |
| G8 fan-in 0 | PASS | **PASS** (`NUL2`/`prototype`/`playwright-mcp` 0) |
| G9 0 eliminados | PASS | **PASS** (S48.1 read-only) |
| G10 no código | PASS | **PASS** |
| G11 `PassPlat 0/0` | PASS | PASS |
| G12 `210/210` | PASS | PASS |
| G13 `CBP 0/0` | PASS | PASS |
| G14 `56/56` | PASS | PASS |
| G15 `InventaNet NO-TOUCH` | PASS | PASS |
| G16 no `push` | PASS | **PASS** |

**S48.0 16/16 PASS → S48.1 16/16 PASS (con corrección).**

---

## 9. Plan S48.1 → S48.2/3

**S48.1 DESIGN COMPLETE** (este doc) → **S48.2 IMPLEMENTATION** → `README.md` + `git rm --cached` **explícito** `87` `ELIMINACIÓN SEGURA` + `.gitignore` + `git commit` + `git push` (sin `force`, sin `CBP`) → **S48.3 CERTIFICATION** → `build 0/0` `210/210` `56/56` + `git status` `master` + `GitHub PassPlat.slnx` sin `CBP`/`bin`/`obj`

**No S48.2 sin S48.1.** `S47.3` `clone` certification pendiente pero `S47` CLOSED.

---

## 10. Referencias

| Archivo | Rol |
|---|---|
| `D:\CODIGOS\PassPlat\README.md` | `# PassPlat` 10 bytes (a reemplazar S48.2) |
| `D:\CODIGOS\PassPlat\PassPlat.slnx` | 29 proyectos, `9e800ef` 1518 tracked |
| `D:\CODIGOS\PassPlat\.gitignore` | 363 líneas, `NOT ignored` `PassPlat.slnx` |
| `D:\CODIGOS\PassPlat\Docs\Sprints\S48\S48.0-GitHub-README-Cleanup-Discovery.md` | Inventario + matriz |
| `PassPlat.Web/PassPlat.Web.csproj` | `net10.0` `MudBlazor 9.8.0` `BlazorMonaco 3.5.0` |
| `D:\CODIGOS\CBP\` | 47 `ProjectReference` `../CBP/` (no publicar) |

